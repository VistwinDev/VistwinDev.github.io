---
title: dev-windows 超級 app(開發型)· 命名空間入口
type: memory-core
app: dev-windows
updated: 2026-06-30
tags: [system, memory, dev-windows]
---

# dev-windows · 開發型超級 app(待補)

> 另一端在 Windows 上建置的「開發型」超級 app。此資料夾是它的記憶命名空間。

## 寫入規則(衝突預防)
- 此 app 的決策 / 情節只放 `dev-windows/決策|情節/`,frontmatter `app: dev-windows`。
- **不要寫進 `manager-mac/`**;跨兩者的原則才放 `共用/`。
- GitHub 用它自己的 repos —— 勿與 manager-mac 的 `vistwin-app` / `vistwin-automation` 混。

## 待補(由 dev-windows 那端填)
- 它是什麼 · 平台/技術棧細節 · repos
- 與 manager-mac 的接點:共用 vault?共用 bridge?還是各自獨立、只共享原則?
