---
title: 維護提案 daily 2026-06-28
type: maintenance-proposal
---

# 🔍 三庫維護提案 — daily 2026-06-28

## daily 日誌 (`daily-journal`)

讀完 Now.md、06-25 daily(最後一篇實檔)、06-27 維護提案、git 軌跡與 health/sprint 資料夾現況。**今天 06-28(日)是 W26 sprint 最後一天**,而 06-26、06-27、06-28 三天都沒有實際 daily 檔(06-27 只產出維護提案,沒寫 daily)。以下為今日 daily 唯讀補寫提案,**未動任何檔案**:

- **進度・先回補 06-26→06-28 三天缺檔**:節奏重啟首篇(06-25)後,06-26 / 06-27 / 06-28 連三天無 daily 實檔。今天 daily 開頭應註明「回補 6/26–6/28」,把期間唯一實際產出(06-27 維護提案三段、`n8n-stack-ROI.pptx` 策略檔於 6/26 出)記成完成項,其餘誠實標空轉,避免又像 W22–W25 累積成 4 週斷檔。

- **🔴 blocker・節奏重啟正式失守**:06-25 daily 把「連續兩天手動跑通」當節奏重啟證據,並訂「先手動跑兩天觀察」。結果 D3(6/26)就斷,連斷三天 → **觀察結論=手動兩天無法續命,節奏重啟首試失敗**。今天 daily 的 blocker 段應誠實下這個判斷,並接著決定:W27 是否就把 cron 派出(原降級給 W27 的議題)從「觀察後再說」改為「必須派,手動已證明不可靠」。

- **🔴 blocker・健檢重跑錨點作廢**:06-25 訂「2026-06-26 重跑 vault-health」為 W26 收束追蹤錨點,但 `Meta/health/` 仍停在 `health-2026-06-24`(latest 未更新),39/100 的位移無從驗證。今天是 W26 最後一天 → daily 應記:**收束日健檢仍未重跑,39→? 無法收尾**,並把「W26 收束健檢」標為今日/收束動作而非靜默跳過。

- **🔴 blocker・發布外洩風險跨 3 日仍未升上 Now.md**:06-25 daily 與 06-27 提案都已主張把「rsync 排除式 blocklist 不看 `publish:`、`03 公司/` 財務合約會上線」列紅燈補進 Now.md,但 Now.md 仍是 6/25 版、完全沒記。今天 daily 應明寫:**此 blocker 已滾 3 天未落地,owner=Francis,動作=開 site repo 確認 Quartz 用 ExplicitPublish vs RemoveDrafts**,並標註「未確認前手動 rsync 視同帶風險」。

- **進度・三項昨日待辦升級為 carry-over D+3**:06-25「明日要做」未勾的三項(showcase 各頁 build-ready 盤點、`_控管/` 定位待 Francis 拍板、跟進 site repo ExplicitPublish)經 6/26–6/28 三天**無一推進**。今天 daily 顯式標「carry-over D+3」,不要靜默複製成新待辦;其中第三項與上條 blocker 是同一件,應合併追。

- **決策待拍板・`_控管/` 雙重真相源收斂**:06-27 weekly 對帳揭出 `_控管/` 物件與 11 篇 `pm:true` 內容筆記並存漂移、`sprint:` 全停 W21(落後 5 週)、`n8n-stack-ROI` 在冷藏區卻帶 `pm:true`。今天是 W26 末日,daily 決策段應把此案點名為 **W27 第一優先**(建議:內容筆記拔 `pm:`、dashboard 只撈 `01 專案/_控管/`、活躍物件 sprint 更新為 W26/W27),待 Francis 拍板。

- **進度・雙軌主線狀態各收一筆**:主線 A(Physical AI)6/24 後整週未再動;主線 B(BIM+DT)整週靜止。今天 daily 應顯式各記一行 **A 軌本週停在 6/24 lab 盤點、B 軌整週零動作**,避免雙軌在無紀錄下被默認支線化(Entity Map 定義 A/B 同級)。

- **流程・今日 daily 同時是 W26 weekly 收束觸發點**:W26(06-22~06-28)今天結束,但 W26 weekly 實檔尚未產出(06-26 只有 weekly 維護提案)。今天 daily 收尾應掛一條「→ 觸發 W26 weekly 收束」的指標,把上面健檢重跑、blocker 升 Now.md、`_控管/` 收斂三件帶進 weekly,否則 W26 會無收束直接滑進 W27。

要不要我接著針對其中某條(例如「節奏重啟失守的判斷段」或「W26 收束指標段」)草擬具體 daily 段落純文字?仍維持唯讀、只出文字、不寫檔。


## Inbox 分類 (`inbox-triage`)

三則 Inbox 檔的 frontmatter 與正規樣板都看完了。三則皆為 `type: maintenance-proposal`、且都**只有 `title`+`type` 兩欄**,而 `Meta/maintenance/` 樣板有完整五欄(`title`/`type`/`created`/`status`/`tags`)。以下為唯讀歸檔提案,**未動任何檔案**:

- **三則全部移出 Inbox → `00 系統/Meta/maintenance/`**:`維護提案-daily-2026-06-25`、`維護提案-weekly-2026-06-26`、`維護提案-daily-2026-06-27` 三則的 `type` 與 6/24 正規檔完全同類,Inbox 只是暫存區不該是終點。移完後 Inbox 回到只剩 `.gitkeep` 的乾淨態(注意:上一輪 triage 只見到前兩則,06-27 那則是新增的,別漏)。

- **檔名統一改 `maint-` 前綴**:正規檔叫 `maint-daily-2026-06-24.md`,三則卻用 `維護提案-daily-…`。提案改名為 `maint-daily-2026-06-25.md`、`maint-weekly-2026-06-26.md`、`maint-daily-2026-06-27.md`,讓 `Meta/maintenance/` 內命名一致、可程式化掃描。

- **三則都缺 `created` / `status` / `tags`,需對齊 6/24 樣板補齊**:目前只剩 `title`+`type`。`created` 各依日期補:`2026-06-25`、`2026-06-26`、`2026-06-27`。

- **`status` 依「提案是否已被消化」分別填,別一律 consumed**:
  - `daily-25` → **`consumed`**:內容已餵進 6/25 daily 實檔(該 daily 已存在)。
  - `weekly-26` → **`pending`**:W26 weekly 實檔尚未產出,且內含發布外洩升 Now.md、`_控管/` 收斂等多項待 Francis 拍板。
  - `daily-27` → **`pending`**:06-27 daily **始終沒寫成實檔**(06-26~28 連三天斷檔),提案內容完全未落地 → 必須標 pending,否則看不出這份還懸著。

- **`tags` 補成樣板格式**(`[system, meta, maintenance-proposal, <週期>]`):`daily-25` 與 `daily-27` 末項填 `daily`;`weekly-26` 末項填 `weekly`。

- **兩則 `pending`(weekly-26、daily-27)等於 W26 收束的待辦旗標**:今天(06-28)是 W26 最後一天,這兩則標 pending 後,正好標示「W26 weekly 尚未寫 + 06-27 daily 內容尚未落地」兩件收束前必須消化的事,建議移檔時保留 `status: pending` 當旗標,而非靜默歸檔。

- **可選・建 `_控管` 或年份子層**:若預期 `maint-*` 檔會持續累積,提案在 `Meta/maintenance/` 下不再加結構(目前量小),維持平鋪即可;僅在檔名前綴 `maint-` + ISO 日期已足夠排序,**不建議**現在就分 daily/weekly 子資料夾,避免過度結構化。

要我針對某一則草擬「移檔後的完整五欄 frontmatter 區塊」純文字版嗎?仍維持唯讀、只出文字、不寫檔。


## 發布快檢 (`publish-quick`)

快檢完成。今天(06-28)重掃全 vault `publish:` 旗標、客戶/私密資料夾、與正典入口檔連結。以下為唯讀提案,**未動任何檔案**:

- **`publish:true` 全 vault 仍只有 `index.md` 一頁,且完全乾淨** — 它無任何 `[[wikilink]]`(內部首頁是純文字「私有,不公開」),不會把內部頁帶上線。**沒有任何客戶/私密頁誤標 `publish:true`** — 誤公開風險:零。grep 命中的其餘檔都是維護提案內文「引述」`publish:` 字串,非 frontmatter 旗標,無誤標。

- **真旗標總數仍是 3 個,旗標根本沒在守門** — 全 vault 只有 `index.md=true`、`98 資料/Strategy/…n8n-stack-ROI.md=false`、`02 技術/Visustwin/INTEGRATION_PLAN.md=false` 三個 `publish:` 鍵,其餘**數百檔無此鍵**。發布走 rsync 排除式 blocklist、不看旗標 → **若 site 端 Quartz 是 RemoveDrafts(預設)而非 ExplicitPublish,所有無旗標檔全部上線**。這仍是跨日未解的 P0,**建議今日 daily 沿用昨日 blocker、不重開新項**,owner=Francis,動作=開 site repo 確認 emitter 模式。

- **四個敏感區實測存在、且零 `publish:` 防護(被動安全)** — `03 公司/`(登記/商標/財務/合約)、`01 專案/寶鋪 showcase/`(客戶交付)、`01 專案/_控管/`(PM 控管物件)、`04 Github/VistwinProject/`(內部專案規格)皆確認存在,全無 `publish:false`。目前安全只是「沒被標 true」被動成立,非主動擋。**提案 defense-in-depth:對這四區批次補 `publish:false`(或 folder-note 壓旗標),使不論 emitter 是哪種都安全** — 這是把外洩從不確定變確定的最低成本動作。

- **`index.md` 文案與實機制仍不符** — 它宣告「本站採 allowlist 發佈」,但 vault 端 rsync 實際不按旗標過濾。**在解掉 emitter blocker 前,別擴大 `publish:true` 的使用**,以免文案承諾落空;也別在未驗證下 flip 任何 showcase 頁為 true。

- **發布面連結乾淨,無明顯壞連結** — `index.md`(唯一公開頁)無 wikilink;正典入口檔 `Now.md` / `CLAUDE.md` 的 8 條連結(Entity_Map、三個 SKILL、W26 daily/README、Repos inventory)今日逐一實測**全部解析正常**。健檢報告的 243 孤兒連結集中在內部檔、不在發布面,**降為內務、非發布紅燈**。

- **預防提醒・showcase 一旦對外要先查連結外溢** — `01 專案/寶鋪 showcase/` 日後若 flip `publish:true`,務必先確認頁內 `[[連結]]` 不會把 `_控管/`、財務、合約等私密頁一併帶上線(交付物常反向連回內部 PM 物件)。

要我產出「該補 `publish:false` 的四區檔案完整清單」或草擬某資料夾的 folder-note + `publish:false` 純文字版嗎?仍維持唯讀、不寫檔。



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/36?signature=4cbe6f904bade83064585b24c4cfaea82d73328a626d43061987dabfc6a05131
