---
title: 維護提案 daily 2026-06-24
type: maintenance-proposal
---

# 🔍 三庫維護提案 — daily 2026-06-24

## daily 日誌 (`daily-journal`)

讀完 `Now.md` 與今天的 daily（`2026-W26/2026-06-25.md`）。今天的 daily 結構已相當完整，但有幾處是「掛了待辦卻沒落結果」的空格。以下為**今天該補的具體提案**（純建議，未動檔）：

- **補「決策」一節 — `_控管/` 層定位**：daily 完成項已記 6/24 新建 `01 專案/_控管/`，但仍標「待釐清為何另立、與 Entity Map 主線 A/B 對應、是否取代舊 sprint 流」。這是結構性決策，建議今天就落一句定論（取代 / 並存 / 試行），否則明天會繼續懸著。

- **補「進度」— showcase build-ready 清單**：完成項裡 showcase 大改但括號註「待補哪些頁已 build-ready、還差什麼」。建議今天補一張小表（數據之門／中控台／沉浸光寓／感應光寓／控制塔 各頁 ✅ready / 🟡待修），讓 6/8 交付後的迭代有可驗收基準。

- **blocker 收斂 — 兩個逾期老項要當場標期**：`DASHBOARD_PAT` 與 org rename(`metaarchetech`→`vistwin-tech`)在 Now/daily 都已掛超過 5 週。建議今天的 daily 直接給三選一處置（重設截止日 / 降級為 backlog / 關閉），而不是再寫進「明日要做」滾動。

- **補「決策」— daily/weekly cron 路線**：在做項標「cron 交 Dispatch 迄今未派出 = 節奏斷 35 天根因」。建議今天落定兩件事：(1) 今日是否手動補跑、(2) cron 走 n8n 那條還是另派，避免又變成只記症狀不記決定。

- **補新 blocker — publish 守門驗證**：daily 已點出 rsync 用排除式 blocklist、不看 `publish:` 旗標，真正守門落在 site repo 的 Quartz ExplicitPublish 而本 vault 無法驗。建議升級為明確 blocker 並指派一個「去 site repo 確認 ExplicitPublish vs RemoveDrafts」的動作項，否則「預設不公開」是空話。

- **補健檢三大失分的本週對應動作**：daily 記了 health 39/100🔴（正典脫節 / 節奏斷 / 243 孤兒連結），但沒對應到今天做了什麼。建議至少補一句「孤兒連結」起手點（例如先掃哪一區），讓健檢分數有可追蹤的改善路徑。

- **收尾 — 心情/能量/一句話仍是 placeholder**：`/10` 與「(等使用者填)」未填。提醒今天補上，這是 daily-weekly-journal skill 的節奏指標，斷填會讓 weekly 回顧少掉趨勢資料。

最關鍵的兩條是**`_控管/` 層定位決策**與**cron 路線決策**——這兩個不落定，明天的 daily 會原封不動再抄一次。


## Inbox 分類 (`inbox-triage`)

讀完 `00 系統/Inbox/`。實際內容只有**一篇**零散筆記：`維護提案-daily-2026-06-24.md`（外加空的 `.gitkeep`）。這篇本身是昨天三庫維護的產出物被丟在 Inbox。以下為**歸檔 + frontmatter 提案**（純建議，未動檔）：

- **【主提案：搬出 Inbox】`維護提案-daily-2026-06-24.md` 不該留在 Inbox** — Inbox 是「未處理零散」暫存區，這篇是已完成的維護提案紀錄。建議移到 `00 系統/Meta/maintenance/`（與 `Meta/health/health-2026-06-24` 同層，維護產出物集中）。若無此資料夾，退而求其次放 `00 系統/Meta/`。

- **【frontmatter 補全】** 目前只有 `title` + `type`，缺日期與標籤、無法被健檢/查詢索引。建議補：
  - `created: 2026-06-24`（或 `date:`，與 vault 其他筆記一致）
  - `tags: [meta/vault, maintenance-proposal, daily]`
  - 保留 `type: maintenance-proposal`（這欄位選得好，建議定為此類產出的標準 type）

- **【內容狀態標記】這篇多數提案「已被消化」** — 對照今天的 daily（`2026-W26/2026-06-25.md`），第 12–28 行的 daily 提案幾乎都已落進今天的 daily。建議補一欄 `status: consumed`（或 `archived`），標明此提案已執行完畢，避免日後誤當待辦再跑一次。

- **【命名一致性】檔名建議對齊 health 報告慣例** — health 報告是 `health-2026-06-24`，這篇是 `維護提案-daily-2026-06-24`。若 maintenance 產出要可批次掃描，建議統一前綴格式（如 `maint-daily-2026-06-24` 或全中文都行，挑一種固定）。非必要，但利於日後 glob。

- **【需求釐清：Inbox 實質為空】** 扣掉這篇產出物，Inbox 沒有真正的隨手記。這與 6/24 健檢「Inbox 0 篇待處理（10/10）」一致。若你近期有桌面/手機隨手記，可能落在外部（Drive / Gmail 草稿 / Apple Notes）而非 vault Inbox —— 要的話下一輪我可去那些來源撈再提案歸檔點（Drive/Gmail MCP 可用）。

- **【流程提醒】維護提案產出位置建議改規則** — 根因是「三庫維護的提案被寫進 Inbox」，導致 Inbox 永遠不會真的清空。建議把這類產出預設寫到 `00 系統/Meta/maintenance/`，Inbox 只留外部進來的原始零散筆記，職責才乾淨。

最關鍵一條是**把這篇移出 Inbox 並標 `status: consumed`**——否則它會一直佔著 Inbox、且內容已過期（提的是「今天 daily 還沒建」，但今天 daily 已經建好了）。


## 發布快檢 (`publish-quick`)

快檢完成。掃了全庫 `publish:` 旗標、客戶/私密區、以及壞路徑連結。結論：**目前沒有誤公開風險，但壞連結是大宗**。以下為提案（純唯讀，未動檔）：

- **【洩漏風險：低】唯一 `publish: true` 是 `index.md`,乾淨** — 全庫只有這一篇對外。內容只有標題＋一句 allowlist 說明,**無任何 `[[連結]]`**(到 Vault Home 是純文字「私有,不公開」),不會把內部頁連帶上線。allowlist 模型成立,維持現狀即可。

- **【誤公開排除】客戶/私密區零 publish 標記** — `01 專案/`(含寶舖 showcase / welltek)、`03 公司/`(登記/商標/財務/合約)全區掃過,**沒有任何 `publish:` 旗標**,沒有誤標公開。寶舖客戶頁安全。

- **【誤判排除】`02 技術/Visustwin/INTEGRATION_PLAN.md` 是 `publish: false`** — 安全。其內文出現的 "publish" 是 pub/sub bridge 術語(producers publish topics),與發布無關,不必處理。

- **【壞連結大宗:50 條 `VisTwin/` → 應為 `Visustwin/`】** 實際資料夾是 `02 技術/Visustwin/`,但全庫有 **50 條** wikilink 指向不存在的 `02 技術/VisTwin/...`(`VisTwin` 少一個 `us`)。散在 `GitHub Overview`、各 Plugin README(`solar.report`/`ai.oracle`/`light.compass`/`ventilation` 等近 20 個)、`sprints/README`、`Backlog`,連 `01 專案/visionbase/README` 與 `寶鋪 showcase/welltek/README` 也中。**建議全庫批次取代 `技術/VisTwin/` → `技術/Visustwin/`**,可一次清掉約 50 個孤兒連結。

- **【壞連結誤報:模板佔位符,勿動】** `Templates/Sprint Planning.md` / `Sprint Retro.md` 的 `{{sprint}}`、`{{prev_sprint}}` 是 Templater 變數(`[[02 技術/VisTwin/sprints/{{sprint}}/planning]]`),非真壞連結。批次取代時這幾條只需修 `VisTwin→Visustwin`,佔位符保留;另建議把 `Templates/` 與 `Meta/health/` 排除在健檢孤兒連結掃描外,避免虛報壓分。

- **【唯一真風險:rsync 是否按 publish 旗標過濾】** vault 根目錄無 quartz/全域發布設定,理論上只 `index.md` 出去;但實際 sync 走手動 rsync(`DASHBOARD_PAT` 未設的 blocker)。**rsync 那條若是整包 `-av --delete` 而非按 `publish:` 過濾,allowlist 形同虛設**。這是目前唯一需要查證的發布風險點——但守門設定在 site repo,本 vault 無法驗,建議列為待辦去 site 端確認用的是 ExplicitPublish。

- **【預防提醒:showcase 對外前先過 guard】** 寶舖 showcase 6/22–24 大改,日後若 flip `publish:true` 對外,務必先檢查頁內 `[[連結]]` 不會把 `_控管/`、財務、合約等私密頁帶上線。目前還沒到這步,但很可能很快會碰。

最高槓桿一條是**批次修 `VisTwin→Visustwin`(50 條)**;最該查證一條是**rsync 是否真按 publish 過濾**。



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/13?signature=e3748a1491e7b6d6e5c5563aa5182cfcc5c85d85c1091f4ff9680e1e3f313db8


寫完只回那個檔案的相對路徑。
