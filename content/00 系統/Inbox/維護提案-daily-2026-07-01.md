---
title: 維護提案 daily 2026-07-01
type: maintenance-proposal
---

# 🔍 三庫維護提案 — daily 2026-07-01

## daily 日誌 (`daily-journal`)

讀完 `Now.md` 與今天兩份 work 記憶(情節「建置 session」+ 決策「核心架構」)。今天 daily 尚未建立,以下是**提案(不寫檔)**:

## 今日 daily(2026-07-01)補寫提案

- **進度|主線推進**:VisTwin Work 從「n8n 能否造 loop agent」一路做到 **Daemon 上線(`engine/server.mjs` :7042,`/dispatch`=Tier C 自我檢核)**、**Tauri 原生 `.exe`(自帶 Vist. icon,Win+Linux 一套)**、**對話可用(`setup-token` → `CLAUDE_CODE_OAUTH_TOKEN`,`/api/chat` 實測回真答案)**——這是整個系統執行層的解鎖點,值得記為今日最大里程碑。

- **進度|框架歸位**:認領家族 **Work 區**,填 `超級 App/Work.md` + 建本記憶命名空間;n8n 部署改為「每區各自跑自己的 Docker、只透過 vault 整合」(作廢先前「共用一個 n8n」)。

- **決策|命題定錨**:VisTwin Work = harness,用架構治 agent 兩弱點(①要催促→n8n 迴圈;②記性差→vault 記憶),兩者咬合成飛輪。四支柱(Windows/n8n/Claude/vault,邏輯只在 Daemon)+ Evaluation Contract 五函式(propose/apply/measure/gate/rollback + toExperience)。建議把決策以「一句命題+連結決策記憶」形式入 daily,細節指回 `2026-07-01_vistwin-work-架構`。

- **踩雷教訓**:`time 0.3.52` 衝突→釘 0.3.51;ChunkLoadError→啟動器乾淨重啟;harness sandbox spawn 的 claude 拿不到使用者憑證(真機驗證須在 user session app 內)。這幾條建議進 daily 以免下次重踩。

- **Blocker / 待續(新)**:①本機 **n8n Docker 尚未安裝**;②Daemon 的 **propose→apply(真自我優化)待白老鼠**;③fleet 即時思考須改用 **TS Agent SDK**(`claude -p` stream-json 不吐 tool_use/thinking);④記憶讀取要切到 `記憶/work/`。

- **Blocker(延續,建議帶進今日)**:`DASHBOARD_PAT` 未設致 sync-to-quartz 仍手動 rsync;**GitHub org rename** `metaarchetech`→`vistwin-tech`(Entity Map 本週行動項,尚未動)。

- **節奏落差提醒**:`Now.md` 仍停在 **2026-06-25(W26)**,「今日 daily log」指標未更新;今天已是 **W27**。建議今日一併把 Now.md 焦點與 daily log 指標更新到 7/1,否則三庫節奏再度斷點。

以上為唯讀提案,未寫入任何檔案。是否要我把其中某幾條實際寫進今天的 daily?


## Inbox 分類 (`inbox-triage`)

讀完 Inbox 全部 6 則零散筆記,並核對正規範本 `Meta/maintenance/maint-daily-2026-06-24.md`(基準格式)。現況:6 則皆 `type: maintenance-proposal`、**只有 `title`+`type` 兩欄**、檔名用 `維護提案-` 前綴(與正規 `maint-` 不一致)、全平鋪在 Inbox。以下是**歸檔提案(唯讀,未動任何檔)**:

## Inbox 歸檔提案(6 則)

- **全部同去向 → `00 系統/Meta/maintenance/`**:6 則皆 `maintenance-proposal`,與該資料夾內既有正規檔 `maint-daily-2026-06-24.md` 完全同類。Inbox 是暫存區非終點,清空後回到只剩 `.gitkeep` 的乾淨態(不需分子資料夾,連基準檔共 7 檔、平鋪排序足夠)。

- **檔名統一改 `maint-` 前綴**:`維護提案-daily-2026-06-25` → `maint-daily-2026-06-25`,`維護提案-weekly-2026-06-26` → `maint-weekly-2026-06-26`,27/28/29/30 比照。使 `Meta/maintenance/` 命名一致、可程式化掃描。**因含 wikilink 風險低(這批無被反向連結),但改名仍屬一次性批次**。

- **補齊缺的三欄 `created / status / tags`(對齊 06-24 基準)**:`created` 各依檔名日期填(`2026-06-25`~`2026-06-30`);`tags` 補成 `[system, meta, maintenance-proposal, <週期>]` —— 五則 daily 末項填 `daily`、`weekly-26` 末項填 `weekly`;`title` 已是「維護提案 daily/weekly YYYY-MM-DD」格式、與基準對齊,可不動。

- **`status` 依「是否已落地實檔」分填,別一律 consumed**:
  - `daily-25` → **`consumed`**(內容已餵進 `2026-W26/2026-06-25.md` daily 實檔)。
  - `weekly-26` + `daily-27/28/29/30` → **`pending`**(06-26→06-30 連 5 天斷檔、W26 weekly 從未寫成實檔,內容均未落地)。這 5 則的 `pending` 正是「斷檔+W26 未收束」的旗標,移檔時務必保留,別靜默歸 consumed 讓斷檔被默認結清。

- **保留各檔末尾的 n8n 核准表單 URL 原文,不清理**:`daily-30`=`form-waiting/95`、`weekly-26`=`form-waiting/34`、`daily-25`=`form-waiting/32` 等。這些屬「待核准狀態」的一部分,尤其 `pending` 檔移檔後仍是待消化事項,連結原文保留。

- **建議一併補 `publish: false`**:這批含 `_控管/`、財務、org 內部代號等敏感內容的引述,且發布守門 blocker(rsync 不看旗標)仍未解 —— 移進 `Meta/maintenance/` 時順手補 `publish: false`,與正在懸置的發布定調脫鉤、不論 emitter 模式都安全。

以上為唯讀提案,未寫入或移動任何檔案。要我針對某一則草擬「移檔後完整 frontmatter 區塊」的純文字版供你過目嗎?(仍不寫檔)


## 發布快檢 (`publish-quick`)

快檢完成:全掃 `publish:` 旗標 + 四個客戶/私密區 + 兩個 `index.md` + 正典檔連結目標。以下**唯讀提案(未動任何檔)**:

## 發布健康快檢

- **誤公開風險為零**:全 vault(排除 `99 封存/`)只有 **3 個 `publish:` 旗標** —— 根 `index.md`=`true`、`98 資料/Strategy/n8n-stack-ROI`=`false`、`02 技術/…/INTEGRATION_PLAN`=`false`。唯一 `publish:true` 的根 `index.md` 內文乾淨(無任何 `[[連結]]`、Vault Home 是純文字),**沒有「標 true 卻屬私密」的誤標檔**。

- **🔴 客戶/私密四區零防護(被動安全,跨日 P0)**:`03 公司/`(登記/商標/財務/合約)、`01 專案/_控管/`(budget/spent 金額)、`01 專案/寶鋪 showcase/`、`04 Github/`(內部 repo 代號)**全區沒有任何 `publish:` 旗標**。安全只靠「沒被標 true」被動成立。**提案 defense-in-depth:對這四區批次補 `publish:false`(或 folder-note 壓旗標),使不論 site emitter 是哪種都安全。**

- **🔴 真風險仍是未解 blocker,非旗標本身**:vault 端 rsync 走排除式 blocklist、**不看 `publish:` 旗標**;守門落在 site repo 的 Quartz emitter。**若 site 是 RemoveDrafts(全鏡像)而非 ExplicitPublish,上述四區數百無旗標檔會全部上線**。本 vault 無法驗 —— owner=Francis,動作=開 site repo 確認 emitter 模式。此項已滾多日仍未上 `Now.md`,建議今日 daily 沿用、並補進「等待/blocked」。

- **⚠️ 第二個 `index.md` 是被漏掉的外溢點**:`00 系統/index.md` 無 `publish:` 鍵(ExplicitPublish 下安全),但**滿是指向正典的 wikilink**(`[[Now]]`、`[[Entity_Map]]`、`[[Vault Home]]`、`[[working-with-frncs]]`…)。**若 site 端全鏡像,這頁會把整張正典導覽圖對外曝光**。建議與敏感四區一起列入補 `publish:false` 對象。

- **文案與實機制不符**:根 `index.md` 宣告「本站採 allowlist 發佈」,但 vault 端 rsync 實際不按旗標過濾。**解掉 emitter blocker 前,別擴大 `publish:true`、也別在未驗證下 flip 任何 showcase 頁為 true**,以免文案承諾落空。

- **壞連結:發布面乾淨**:抽驗正典檔連結目標全部命中實檔 —— `00 系統/index.md` 的 `[[Vault Home]]`/`[[Entity_Map]]`/`[[超級 App/README]]`/`[[技能/_index]]`,`Now.md` 的 `[[vault-publish-pipeline/SKILL]]`/`[[2026-W26/README]]`/`[[2026-06-25]]`/`[[2026-05-25_inventory]]`,`CLAUDE.md` 的 `[[working-with-frncs/SKILL]]` **均存在、無壞連結**。健檢基線 ~243 孤兒連結集中在內部檔、不在發布面,維持「內務待清、留 weekly 專項處理」即可,**不列今日發布紅燈**。

以上為唯讀提案,未寫入或修改任何檔案。要我把「該補 `publish:false` 的四區 + `00 系統/index.md` 完整清單」草擬成純文字供你過目嗎?(仍不寫檔)



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/242?signature=7760e174a61c4f7d53065ed80026c0a8e1b982f8abe91cd5aaab51b057bc21b2
