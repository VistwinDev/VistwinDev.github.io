---
title: 維護提案 daily 2026-06-30
type: maintenance-proposal
---

# 🔍 三庫維護提案 — daily 2026-06-30

## daily 日誌 (`daily-journal`)

讀完 `Now.md`、`daily-weekly-journal` SKILL、W26 sprint(實檔仍停在 06-25)、今日無 code commit(僅 vault backup)、及 06-29 提案。以下是**今天 daily 該補的具體提案**(唯讀,未動任何檔):

- **進度・誠實標斷檔 D+5,別靜默搬待辦**:今天是 W27 第二天,最後實檔 daily 停在 06-25,06-26→06-29 連四天斷檔。今天 daily 開頭註明「回補 6/26–6/30」,期間唯一真實產出(06-27/28/29/30 四份維護提案 + 每日 vault backup)記為完成項,其餘誠實標空轉 — 別重演 W22–W25 假進度。

- **🔴 blocker・cron 派出已從「議題」升為「節奏存活前提」**:06-25 訂「手動跑兩天觀察」,結果連斷四天,「手動不可續命」已被證偽。今天 blocker 段把 daily/weekly 兩個 cron(n8n Dispatch)寫死為 **W27 必做**,owner/車道固定,不再每日懸置。

- **🔴 blocker・發布外洩風險滾到 D+5 仍未升上 Now.md**:rsync 走排除式 blocklist 不看 `publish:`,若 site 端 Quartz 是 RemoveDrafts 而非 ExplicitPublish,`03 公司/` 財務合約恐全上線。daily 明寫 **owner=Francis、動作=開 site repo 確認 emitter 模式;未確認前手動 rsync 視同帶風險**,並把此條一併補進 `Now.md`「等待/blocked」(已三度被提仍未上 Now)。

- **🔴 開週動作・W27 資料夾尚未建**:`sprints/` 只到 W26,**今天寫 daily 前需先確認/建 `2026-W27/` README 與歸屬**。同時點名 W26 收束兩件未結:① W26 weekly 實檔從未寫(只有 pending 提案);② 06-26 訂的「收束日重跑 vault-health」未跑,health 仍停 39/100 @06-24。

- **決策待拍板・`_控管/` 雙重真相源收斂列 W27 首案**:`_控管/` 物件與 11 篇 `pm:true` 內容筆記並存漂移、`sprint:` 全停 W21(落後 6 週)、`n8n-stack-ROI` 在冷藏區卻帶 `pm:true`。建議方向(內容筆記拔 `pm:`、dashboard 只撈 `01 專案/_控管/`、活躍物件更新 sprint),待 Francis 拍板。

- **進度・雙軌主線各補一行**:主線 A(Physical AI)6/24 lab 盤點後整週未動、主線 B(BIM+DT)整週零動作,各記一行,避免兩軌在無紀錄下被默認支線化(Entity Map 定義 A/B 同級)。

- **carry-over・06-25「明日要做」三項升 D+5**:showcase 各頁 build-ready 盤點、`_控管/` 定位待 Francis、跟進 site repo ExplicitPublish — 四天無一推進,顯式標「carry-over D+5」,第三項併入上方發布 blocker 追,不重開新項。

要我把其中任一條(例如「W27 正式開週段」或「節奏證偽 + cron 必做段」)草擬成可直接貼入的 daily 段落純文字嗎?仍維持唯讀、不寫檔。


## Inbox 分類 (`inbox-triage`)

已讀 Inbox 全部 6 則與正規範本 `Meta/maintenance/maint-daily-2026-06-24.md`(五欄 frontmatter:`title / type / created / status / tags`,`maint-` 前綴)。6 則目前全部只有 `title`+`type` 兩欄。以下純文字歸檔提案,**未動任何檔**:

- **6 則全部同類、同一去向 → `00 系統/Meta/maintenance/`**:`維護提案-daily-2026-06-25/27/28/29/30` + `維護提案-weekly-2026-06-26`,皆 `type: maintenance-proposal`,與 6/24 正規檔完全同類。Inbox 是暫存區、非終點,清空後回到只剩 `.gitkeep` 的乾淨態。

- **檔名統一改 `maint-` 前綴**:`維護提案-daily-2026-06-25.md` → `maint-daily-2026-06-25.md`、`維護提案-weekly-2026-06-26.md` → `maint-weekly-2026-06-26.md`,27/28/29/30 比照,讓 `Meta/maintenance/` 命名一致、可程式化掃描。

- **6 則都補齊缺的三欄 `created / status / tags`**:`created` 各依檔名日期填 `2026-06-25`～`2026-06-30`;`title` 可選同步去空格對齊 6/24 的「維護提案 daily YYYY-MM-DD」格式(不強制)。

- **`status` 依「是否已落地實檔」分填,別一律 consumed**:
  - `daily-25` → **`consumed`**(已餵進 `2026-06-25.md` daily 實檔)。
  - `weekly-26` + `daily-27/28/29/30` → **`pending`**(06-26→06-30 連五天斷檔 + W26 weekly 從未寫成實檔,內容均未落地)。

- **`tags` 補成樣板格式 `[system, meta, maintenance-proposal, <週期>]`**:五則 daily 末項填 `daily`、`weekly-26` 末項填 `weekly`。

- **五則 `pending` 即斷檔/W26 收束旗標,移檔務必保留 `status: pending`**:這正好標示「W26 weekly 未寫 + 06-27~30 daily 內容未落地」這批待消化事項,別靜默歸成 consumed 而讓斷檔被默認結清。

- **`06-30` 這份含未完成的核准連結,移檔保留原文**:末尾有 `localhost:5678/form-waiting/42` 核准表單 URL;歸 `pending` 時保留原文即可,不必清理連結(屬待核准狀態的一部分)。

- **暫不加子資料夾結構**:`maint-` 前綴 + ISO 日期已足夠排序,目前量小(連 6/24 共 7 檔),`Meta/maintenance/` 維持平鋪即可,避免過度結構化。

要我針對某一則草擬「移檔後完整五欄 frontmatter 區塊」純文字版嗎?仍維持唯讀、只出文字、不寫檔。


## 發布快檢 (`publish-quick`)

發布快檢完成。重掃全 vault `publish:` 旗標、唯一公開頁 `index.md` 連結、四個客戶/私密區。純文字提案,**未動任何檔**:

- **`publish:true` 全 vault 仍只有 `index.md` 一頁,且完全乾淨**:`index.md` 內文 `[[wikilink]]` 數 = 0(「內部首頁 → Vault Home(私有,不公開)」純文字,非連結)。**沒有任何客戶/私密頁誤標 `publish:true` — 誤公開風險:零。** 其餘 `publish:` 旗標僅 `98 資料/Strategy/2026-06-25_n8n-stack-ROI.md=false`、`02 技術/Visustwin/INTEGRATION_PLAN.md=false`,皆 false,正確。

- **真旗標總數仍只有 3 個,旗標根本沒在守門(跨日 P0,沿用 blocker、不重開新項)**:全 vault 僅上述 3 鍵,其餘數百檔無 `publish:` 鍵。發布走 rsync 排除式 blocklist、不看旗標 → **若 site 端 Quartz 是 RemoveDrafts(預設)而非 ExplicitPublish,所有無旗標檔全部上線**。owner=Francis,動作=開 site repo 確認 emitter 模式。

- **四個敏感區實測存在、且零 `publish:` 防護(被動安全)**:`03 公司/`(登記/商標/財務/合約)、`01 專案/寶鋪 showcase/`(客戶交付)、`01 專案/_控管/`(PM 控管)、`04 Github/`(內部規格)皆確認在位,`publish:false` 命中 = 0。目前安全只靠「沒被標 true」被動成立、非主動擋。**提案 defense-in-depth:對這四區批次補 `publish:false`(或 folder-note 壓旗標),使不論 emitter 是哪種都安全** — 把外洩從不確定變確定的最低成本動作。

- **`index.md` 文案與實機制不符**:第 13 行宣告「本站採 allowlist 發佈」,但 vault 端 rsync 實際不按旗標過濾。**在解掉 emitter blocker 前,別擴大 `publish:true` 使用、也別在未驗證下 flip 任何 showcase 頁為 true**,以免文案承諾落空。

- **發布面連結乾淨、無壞 `[[連結]]`**:唯一公開頁 `index.md` 內 wikilink = 0,發布面不存在壞連結或反向帶出私密頁的風險。內部孤兒連結(健檢 ~243 條)集中在內部檔、不在發布面,維持「內務待清」即可,不列今日發布紅燈。

- **預防提醒・showcase 日後對外要先查連結外溢**:`01 專案/寶鋪 showcase/` 若日後 flip `publish:true`,務必先確認頁內 `[[連結]]` 不會把 `_控管/`、財務、合約等私密頁反向帶上線(交付物常反連回內部 PM 物件)。

要我產出「該補 `publish:false` 的四區檔案完整清單」或草擬某資料夾的 folder-note + `publish:false` 純文字版嗎?仍維持唯讀、不寫檔。



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/49?signature=e43f213bc703dbb5cd4e605e531bdcef1a10ca9363a2cedc2da44b3b681f4db0
