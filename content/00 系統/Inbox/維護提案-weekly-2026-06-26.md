---
title: 維護提案 weekly 2026-06-26
type: maintenance-proposal
---

# 🔍 三庫維護提案 — weekly 2026-06-26

## weekly 總結+Now (`weekly-summary`)

已彙整本週(W26,6/22–6/28)的軌跡。本週實質只有 **1 篇 daily(6/25,回補 6/22–6/24)**,但內容密度高。以下為唯讀提案,**未動任何檔案**:

## Weekly 總結提案(W26)

- **節奏重啟是本週主成果** — daily 連續兩天(6/24–6/25)手動跑通,直接命中健檢三大失分之一「節奏斷」(基線 39/100)。提案 weekly 標題定調為「斷檔 35 天後的節奏重建週」,並把 **6/26(今天)的健檢重跑**寫成週收束的追蹤錨點 — daily 已預約此日重跑,weekly 應驗證 39→? 的位移,否則錨點落空。

- **⚠️ 最高優先:把發布外洩 BLOCKER 升上 weekly 與 Now.md** — daily 揭露 rsync 用排除式 blocklist、不看 `publish:` 旗標,`03 公司/`(財務/合約)、寶舖 showcase、`_控管/` 都未被排除;守門只能落在 site repo 的 Quartz ExplicitPublish,本 vault 無法驗。這是真實會把財務合約帶上線的風險,但 **Now.md 目前完全沒記**。提案:weekly 列為本週唯一紅燈,Now.md「等待/blocked」段補一條,owner = Francis,動作 = 開 site repo 確認 ExplicitPublish vs RemoveDrafts。

- **兩件待 Francis 拍板的結構決策要在 weekly 點名,避免跨週懸置** — (1) `_控管/` 控管層(6/24 新建)與 Entity Map 主線 A/B 的對應、是否取代舊 sprint 流;(2) 寶舖 showcase 各頁 build-ready 狀態未盤點成清單。兩者 daily 已標「待釐清」,提案 weekly 明確收斂為「W27 第一優先決策項」。

- **主線 A Physical AI 有實質進展,值得在 weekly 獨立記一筆** — 6/24 落地 Linux_Lab_Plan、kuka-control-feasibility、network-inventory、visionbase-monitor,主線 A 從規劃進入 lab/網路盤點階段。主線 B(BIM+DT)本週未動 — 雙軌中 B 靜止,提案 weekly 顯式註明,以免誤判為支線化。

## Now.md 更新提案

- **更新時間戳與焦點段** — 第 3 行改為「2026-06-26(W26 weekly 收束)」;第 22 行「daily/weekly 自動化 cron」明確標為 **W27 議題**(daily 已定調 cron 降級),不再掛「進行中」。

- **「等待/blocked」段重寫兩條既有項+補一條新項** — DASHBOARD_PAT(第 26 行)改為「**待 site 端守門確認後再決定是否設**,短期維持手動 rsync」;GitHub org rename(第 27 行)從「本週內」改為「**backlog,不訂死期,與 vault 資料夾 rename 同波處理**」;**新增**上述發布外洩 BLOCKER 為清單最上方紅燈項。

- **今日 daily log 段(第 33–34 行)** — 若今日(6/26)會產出 daily/健檢重跑,提案補上 `[[…/2026-06-26]]` 連結;weekly 收束後此段應改指向 W26 README 而非單篇 daily。

需要我據此草擬 weekly 檔與 Now.md 的具體 diff(仍唯讀、只出文字)嗎?或你要先就 BLOCKER 那條拍板再往下?


## 看板對帳 (`pm-reconcile`)

三處對帳完成(9 個 `_控管/` 物件 ↔ 11 篇帶 `pm:` 的內容筆記 ↔ Now.md)。以下為唯讀提案,**未動任何檔案**。對齊基準是 `_控管/_schema.md`。

## 結構性不一致(最該先處置)

- **雙重真相源:每個專案同時有 `_控管/` 物件 + 內容筆記都掛 `pm: true`** — schema 明文「內容/知識放各專案資料夾,`_控管/` 只放控管欄位 + `ref` 連回」。但 `寶鋪 showcase/README`、`visionbase/README`、`Physical_AI/README` 等 11 篇內容筆記全部 opt-in `pm: true`,會被 dashboard 二次撈取,與 `_控管/` 物件並存漂移。**提案:擇一收斂** — 要嘛內容筆記移除 `pm:` 系列欄位(只留 `ref` 被指向),要嘛把 dashboard 撈取範圍限定在 `01 專案/_控管/`。建議前者,符合 schema 原意。

- **內容筆記全數缺 `type:`(schema ✅必填),且出現 enum 外的值** — 11 篇 `pm:true` 內容筆記沒有一篇宣告 `type`(project/task/milestone);`Physical_AI/README` 用 `status: active`、`Linux_Lab_Plan` 用 `status: baseline`,均不在 `todo/in-progress/blocked/done` enum 內;`Physical_AI/README` 的 `priority: P0 (5-10 yr)` 不是裸 `P0`。**提案:若保留這些為控管物件,補 `type` 並把 status/priority 改回 enum;否則連同上一條一併拔除 `pm:`**。

- **`98 資料/Strategy/2026-06-25_n8n-stack-ROI.md` 帶 `pm: true` + `type: strategy-note`** — 它在冷藏區卻會洩進 dashboard,且 `type` 非法。**提案:移除該檔 `pm:` 旗標**(strategy note 不是控管物件)。

## 欄位漂移(對帳出的具體差值)

- **`sprint:` 全面停在 `2026-W21`,但 Now.md / daily 已在 W26** — `_控管/` 的 寶鋪、寶鋪·B-TV、寶鋪·B-Table、vault_infra、vistwin-pm 五個物件 `sprint: 2026-W21`,落後 5 週。**提案:活躍物件 `sprint` 更新為 `2026-W26`**(已 done 的 B-TV / 6-8交付可不動或標歷史 sprint)。

- **同名專案 progress 在兩處須鎖定單一來源** — 目前 `_控管/寶鋪.md`(75)↔ `README`(75)、`_控管/visionbase.md`(35)↔ `README`(35)、`_控管/Physical_AI.md`(10)↔ `README`(10) **數值雖巧合相等**,但分散兩處遲早分叉;另 `visionbase/visionbase-monitor`(40)、`kuka-control-feasibility`(30)、`network-inventory`(done/100)、`Physical_AI/Linux_Lab_Plan`(20)都是**只存在於內容筆記、`_控管/` 無對應 task 物件**的「無父控管項」。**提案:這些 lab 子項若要進甘特,於 `_控管/` 補對應 `type: task` 物件(帶 `parent`/`start`/`end`),否則拔 `pm:` 退回純內容**。

## Now.md ↔ _控管 對帳

- **Now.md 漏列兩個 in-progress 專案** — `_控管/` 有 `vistwin-pm`(P1, 50%, dashboard 本體)與 `visionbase` lab 建置(P1, 35%),Now.md「進行中」段都沒提。反向地,Now.md 把「Repos 大整理」列進行中,但 `_控管/` 無對應物件。**提案:Now.md「進行中」補上 vistwin-pm 與 visionbase;或在 `_控管/` 補 Repos 物件,使兩處專案清單對齊**。

- **實體命名 寶鋪/寶舖 不一致,且預算 rollup 對不齊** — `_控管/` 檔名與 title 用「寶**鋪**」,Now.md / daily 用「寶**舖**」(Entity Map 應指定唯一寫法)。另父項 `寶鋪.md` spent 850k,子項 B-TV(60k)+ B-Table(50k)= 110k,父子支出無法對帳(外包未細分)— schema 只擋「子項預算超額」(140k<1.2M 通過),但 spent 不彙整建議在備註標明,避免誤讀為總支出。

要我把以上挑其中一條(例如「雙重真相源」收斂方向)展開成逐檔的具體 diff 草案嗎?仍唯讀、只出文字。


## Repos 盤點 (`repos-inventory`)

已讀 `2026-05-25_inventory.md`(唯一一份,距今 **32 天**)。它的基準是舊 Windows host(`D:\Visustwin\*`)、舊 org `metaarchetech`、且 8 個 repo 的 sync 欄是 PAT 403 下的 cache 推測。以下為「下次套用時怎麼重抓 + 改什麼」的提案,**未動任何檔案、未抓即時 git**:

- **新檔不要覆蓋舊檔,開 `2026-06-26_inventory.md` 並保留 5/25 為歷史基線** — 兩份對照才看得出 6 個紅項(R1–R6)這 32 天是收掉還是惡化。在新檔 frontmatter 補 `supersedes: 2026-05-25_inventory`,Now.md 第 23 行的 inventory 連結改指新檔。

- **Host 基準已換,`D:\Visustwin\*` 路徑全面失效,需在當前 host 重掃** — 本 session 在 macOS(`/Users/visionbase/Desktop/VisTwin/`),舊檔 header `host: VisTwin (D:\VisTwin)` 與所有 `D:\…` 路徑都要重抓。**提案:套用時先確認 repos 實際落地路徑(Mac 端 clone 了哪些 / 是否仍在 Windows 機),inventory 改成相對 repo 名 + 標註 host,不再寫死碟符**,否則跨機永遠對不上。

- **org rename 會讓所有 remote URL 失效,inventory 要與 rename 同一波處理** — Now.md / 6-25 daily 都有 `metaarchetech` → `vistwin-tech`(現已降 backlog)。舊檔 16 個 remote 全是 `github.com/metaarchetech/*`。**提案:新檔的 remote 欄先標 `(rename pending)`,等 rename 落地當天一次性重抓 remote + 預設分支**,避免抓了又作廢。

- **PAT 盲區是舊檔最大失真源,重抓前先修授權** — 舊檔 8 個 VisTwin repo 因 fine-grained PAT 沒授權回 403/404,sync 欄全是 cache 推測(非即時 truth)。**提案:套用時先 `gh auth status` / 換 org-wide PAT,讓 8 個 repo 的 behind/ahead 拿到真值**;拿不到就在每列明確標 `sync=cache-only`,不要再讓推測值混進總覽看似可信。

- **vault repo(obsidian)那列已過時,改記「daily 自動備份」現況** — 舊檔 R3 說 vault「behind 2、obsidian-git 沒拉」;但近期 git log 是連續 `vault backup: 2026-06-25 …` 自動 commit,節奏已不同。**提案:重抓本 repo 真實 ahead/behind,R3 多半可降級或關閉**。

- **補上舊檔漏列的寶舖子 repo 與 deployment bundle 的後續** — `_控管/` 已有 `寶鋪·B-TV`、`寶鋪·B-Table`(各自「獨立 repo」,B-TV 起 6/05,在 5/25 掃描之後),舊 inventory 沒有這兩個 repo;另 §6 的 `baopu-v0.1` deployment bundle(非 git)當時懸而未決。**提案:新檔把 B-TV / B-Table repo 納入掃描清單,並回填 baopu bundle 最終去向(建 repo / 併入 / 維持本機)的決議**。

- **標色重排序的 anchor 要更新** — 舊檔 §2 用「主線 A/B + 寶舖 6/6」排序,但寶舖已 6/8 交付、主線 A 進到 lab/網路盤點階段(Physical_AI / visionbase 落地文件)。**提案:重抓後依現行 Entity Map 主線 A/B + W26 焦點重排紅黃清單**,讓「要立刻處理」對得上當前戰略而非一個月前的。

要我把這些整理成新 inventory 的**空白模板骨架**(欄位、區塊、待抓欄位留 `TODO`),方便套用時直接填真實 git 狀態嗎?仍唯讀、只出文字。


## 發布盤清 (`publish-audit`)

本機證據齊了。發布全鏈盤清提案如下,**未動任何檔案、未觸發任何 sync**。最關鍵的發現實證了 6/25 daily 的 BLOCKER:

## Sync 狀態([2] 段)

- **[2] 自動段仍斷,且 SKILL 本身已過期到不能照跑** — `DASHBOARD_PAT` 未設(daily 證實逾 5 週),sync-to-quartz 停用,靠手動 rsync 代跑,此狀態未變。但更嚴重的是 **SKILL 全文是舊 host `D:\VisTwin\obsidian` + 舊 org `metaarchetech`**,現在已在 macOS(`/Users/.../vistwin-vault`)且 org rename pending —— SKILL 自己的 `revisionTrigger`「repo 遷移到新 org」已觸發,`lastReview 2026-05-20` 過期。**提案:SKILL 標 `status: needs-revision`,Pipeline 圖、Pre-flight 路徑、所有 `gh api repos/metaarchetech/...` 指令在 rename 落地時一次性改寫**,否則照著跑會全 404。

## 排除清單(核心風險 — 實證 BLOCKER)

- **守門機制定調錯誤已被數據坐實:207 篇 .md 只有 1 篇 `publish:true`、3 篇有任何 `publish:` 旗標** — 若 Quartz 真用 ExplicitPublish(只 emit `publish:true`),站點會幾乎全空,與「full mirror」現況矛盾 → 實際必然是**全鏡像(RemoveDrafts 式),守門純靠 rsync exclude blocklist、完全不看 `publish:` 旗標**。這就是 daily BLOCKER 的鐵證。**提案:二選一定調並寫進 SKILL** —— (a) site 改 ExplicitPublish + 對要公開頁補 `publish:true`(目前僅 1 篇,工程量大但最安全);或 (b) 維持全鏡像,但 exclude 清單必須補齊到涵蓋所有敏感區(見下)。在定調前,**手動 rsync 應暫停或先 `--dry-run` 核對**。

- **SKILL 的 exclude 清單同時「列了不存在的、漏了真敏感的」** — 清單裡的 `private` / `templates` / `.claude` / `.trash` 在本 vault **根本不存在**(舊 layout 殘留,空保護);而真正含機密的 `03 公司/`(財務/合約)、`01 專案/_控管/`(budget/spent 金額)、`寶鋪 showcase`、`04 Github/`**全部不在 exclude 內**,當前會被 `--delete` 全鏡像上站。**提案:exclude 清單重寫,以現行 7 區實況為基準,明列 `03 公司`、`01 專案/_控管`、`04 Github` 為必排除**。

- **新增的 `04 Github/` 是未文件化的第 7 區,且含內部代號** — CLAUDE.md 仍寫「6 區」,但根目錄實為 7 個編號區;`04 Github/` 內是 repo 清單筆記(`BF-NFC-abandoned`、`B-TV`、`B-Table`、`vistwin-vault` 等內部開發代號)。它未被任何 exclude 涵蓋。**提案:(1) CLAUDE.md「6 區」更新為含 `04 Github` 的現況;(2) 此區判定 publish 與否並據此加 exclude 或補 `publish:false`**。

## 壞連結

- **健檢基線 243 孤兒連結尚未起改善,且 SKILL/Now 內部參照本身已 stale** — health-2026-06-24 把「243 孤兒連結」列為三大失分之一,本週(W26)只動了「節奏斷」一項,連結項未碰。另 SKILL「Related」與 Pipeline 圖內嵌的 `D:\` 路徑、`metaarchetech` remote 都對不上現況,屬自我參照的壞連結。**提案:把孤兒連結掃描納入 weekly 例行(可掛 `scripts/vault-health.mjs`),先從正典檔(CLAUDE.md / Now.md / 各 SKILL)的失效內部連結修起**,因為這幾份是下游判斷的基準,壞連結傳染性最高。

兩條最高優先:**(1) exclude 清單在敏感區補齊前,手動 rsync 先停或 `--dry-run`;(2) 開 site repo 確認 Quartz 是 ExplicitPublish 還是 RemoveDrafts —— 這一條沒驗證,發布外洩風險就一直在(owner: Francis,本 vault 不可驗)。**

要我把「重寫後的 exclude 清單(對齊現行 7 區)」草擬成具體 `--exclude` 列表給你過目嗎?仍唯讀、只出文字。


## 筆記衛生 (`note-hygiene`)

掃描完成(注:完整反向連結圖的 Python 被權限擋下,孤兒部分改以逐檔 grep 抽驗,非全量;frontmatter 與命名為全量)。以下唯讀提案,**未動任何檔案**:

- **命名最嚴重:寶**舖**/寶**鋪** 全 vault 分裂(舖 59 檔 ↔ 鋪 43 檔)** — 連資料夾 `寶鋪 showcase/`(鋪)與其內檔 `寶舖品牌概念對照表`(舖)、`_控管/寶鋪.md`(鋪)都打架,跨 weekly 任務反覆出現。**提案:先到 [[00 系統/Entity_Map]] 拍板 canonical 寫法,再全域統一(含資料夾名與 `_控管/` title);因牽動 wikilink,改名需連帶更新指向,屬一次性批次而非逐檔隨手改**。

- **產品名四拼法並存:visustwin(610)/VisTwin(475)/Visustwin(249)/VisusTwin(2)** — folder 級就有 `02 技術/Visustwin`、`01 專案/VisTwin`、`99 封存/…/VisusTwin`、`99 封存/機台/visustwin`,CLAUDE.md 正典用 **VisTwin**。**提案:Entity_Map 明訂「文檔/資料夾/實體名一律 VisTwin,plugin id / 程式 namespace 保留小寫 `visustwin` 為白名單例外」,避免把 610 個技術 id 也一起誤改**。

- **孤立資料夾 `01 專案/VisTwin/` 只剩 1 檔、且應併入 Physical_AI** — `Physical_AI_Chapter_Draft_v0.md` 唯一反向連結來自 `01 專案/Physical_AI/Linux_Lab_Plan`,內容屬主線 A,卻獨佔一個與舊產品名同名的空殼夾。**提案:移入 `01 專案/Physical_AI/`,刪除空的 `01 專案/VisTwin/`,順手消除與 `02 技術/Visustwin` 的視覺混淆**。

- **同一批實體被三處平行追蹤,副本會分叉** — repo 狀態散在 `98 資料/Repos_State/`、`04 Github/`、`02 技術/Visustwin/Web/*`;寶舖展間 A–G 代號散在 `寶鋪 showcase/軟體專案/`、`硬體採購/`、`04 Github/VistwinProject/`(B-TV/B-Table/F-table/F-wall…),同一展間出現不同代號系統。**提案:Entity_Map 為「repo 清單」與「展間」各指定單一 SoT,其餘改 `ref` 連回,避免三套孤兒副本各自漂移**。

- **18 檔完全無 frontmatter,含正典 `Now.md`** — Now.md(下游判斷基準)、`01 專案/CLAUDE.md` 及多篇 `98 資料/Lived` 知識卡都無 frontmatter。這與發布鏈直接相關:無 `publish:` 旗標的檔在「全鏡像」下會預設上站。**提案:至少對 Now.md 補最小 frontmatter(created/tags),對會上站的 Lived 知識卡補 `publish:` 判定,與發布盤清那條的守門定調綁一起做**。

- **`A-數據之門` 單一展間碎成 4 檔、命名無規則** — `軟體專案/` 下有 `A-數據之門.md`、`A-數據之門-2.md`、`A-數據之門-總覽與坑.md`,加 `硬體採購/A-數據之門.md`。`-2` 是無語意尾碼。**提案:以 `-總覽與坑` 為 hub,其餘標 `parent` 連回或合併,`-2` 重命名為有意義標題**。

- **把這幾類掃描制度化,別每週手動 grep** — health-2026-06-24 已報 243 孤兒連結但本週未動。**提案:將「無 frontmatter / 0 inbound / 命名變體(寶舖·VisTwin 拼法)」做成 `scripts/vault-health.mjs` 固定報表,weekly 自動產出**,讓孤兒/命名稽核變可追蹤的數字而非一次性人工抽查。

要我針對其中一條(例如 Entity_Map 的 canonical 命名定義 + 白名單例外清單)草擬具體文字給你過目嗎?仍唯讀、只出文字。



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/34?signature=4bc5b576d61b0f535940a10277d79685556fd110dd571890574f9bd79ee8270c


寫完只回那個檔案的相對路徑。
