---
title: 維護提案 daily 2026-06-27
type: maintenance-proposal
---

# 🔍 三庫維護提案 — daily 2026-06-27

## daily 日誌 (`daily-journal`)

讀完 Now.md、06-25 daily、06-26 weekly 提案,與 git 軌跡。**今天(06-27)還沒有 daily 檔,且 06-26 也沒產出 daily**——這是提案的核心背景。以下為唯讀提案,未動任何檔案:

## 今日 daily(2026-06-27)補寫提案

- **進度・先回補 06-26 缺檔**:06-26 只產出 weekly 維護提案 + `n8n-stack-ROI.pptx` 策略檔,**沒寫 daily**。今天 daily 開頭應註明「回補 6/26」,把這兩件記成完成項,避免又像 W22–W25 那樣跨日累積成斷檔。

- **⚠️ blocker・節奏在第 3 天就斷了**:06-25 daily 自我宣稱「連續兩天手動跑通、節奏重啟」,但 6/26 沒接上 → 「連兩天」沒能變「連三天」。提案在 blocker 段誠實記一條:**節奏重啟首試於 D3(6/26)失守**,並寫下今天是否重新接續,否則「節奏斷」這項健檢失分等於沒改善。

- **🔴 blocker・健檢重跑錨點落空**:06-25 把「2026-06-26 重跑 vault-health」訂為週收束追蹤錨點,但 `Meta/health/` 仍停在 `health-2026-06-24`(latest 未更新)。提案記:**6/26 健檢未重跑,39→? 的位移無法驗證**,需在 6/28 weekly 收束前補跑,否則錨點作廢。

- **🔴 blocker・發布外洩風險仍未升上 Now.md**:weekly 提案已主張把「rsync 排除式 blocklist 不看 `publish:` 旗標、`03 公司/` 財務合約會上線」列為紅燈並補進 Now.md,但 Now.md 目前仍是 6/25 版、完全沒記。提案 today daily 明寫:**此 blocker 跨日未落地到 Now.md,owner=Francis,動作=開 site repo 確認 Quartz 用 ExplicitPublish vs RemoveDrafts**。

- **進度・三項昨日待辦原樣滾動**:06-25「明日要做」的三項(showcase 各頁 build-ready 盤點、`_控管/` 層定位拍板待 Francis、跟進 site repo ExplicitPublish)經過 6/26 一天**無一推進**。提案在 daily 顯式標為「carry-over D+2」,不要靜默複製成新待辦。

- **決策待拍板・pm-reconcile 揭出的雙重真相源**:weekly 對帳發現 `_控管/` 物件與 11 篇 `pm:true` 內容筆記並存漂移、`sprint:` 全停在 W21(落後 5 週)、`n8n-stack-ROI` 在冷藏區卻帶 `pm:true`。提案 today daily 在決策段點名:**擇一收斂(建議內容筆記拔 `pm:`,dashboard 只撈 `01 專案/_控管/`)+ 活躍物件 sprint 更新為 W26**,列為 W27 第一優先,待 Francis 拍板。

- **進度・主線狀態收一筆**:主線 A(Physical AI)6/24 後本週未再動;主線 B(BIM+DT)整週靜止。提案在 daily 顯式註明 **B 軌靜止**,以免雙軌被誤判為支線化(weekly 提案亦有同一顧慮)。

要不要我接著針對其中某條(例如「節奏斷 D3」或「健檢重跑」)草擬具體 daily 段落文字?仍維持唯讀、只出文字、不寫檔。


## Inbox 分類 (`inbox-triage`)

讀完 Inbox:除 `.gitkeep` 外只有兩則「零散筆記」——都是維護提案產物,且**已有正規歸宿**:`00 系統/Meta/maintenance/`(裡面 6/24 那篇 `maint-daily-2026-06-24.md` 是對照樣板)。以下為唯讀提案,未動任何檔案:

## 歸檔去向

- **兩則都應移出 Inbox → `00 系統/Meta/maintenance/`**:`維護提案-daily-2026-06-25.md` 與 `維護提案-weekly-2026-06-26.md` 的 `type: maintenance-proposal` 與 6/24 正規檔完全同類,Inbox 只是暫存,不該是它們的終點。移過去後 Inbox 回到只剩 `.gitkeep` 的乾淨態。

- **檔名統一成 `maint-` 前綴**:正規檔叫 `maint-daily-2026-06-24.md`,但這兩則用 `維護提案-daily-…`。提案改名為 `maint-daily-2026-06-25.md`、`maint-weekly-2026-06-26.md`,讓 `Meta/maintenance/` 內命名一致、可程式化掃描。

## 缺漏的 frontmatter(對齊 6/24 樣板)

- **兩則都缺 `created` / `status` / `tags`,只剩 `title`+`type`**:6/24 樣板有完整五欄。提案 daily-25 補 `created: 2026-06-25`、weekly-26 補 `created: 2026-06-26`。

- **`status` 依消化狀態分別填**:daily-25 的內容已餵進 6/25 daily(該 daily 檔已存在)→ 標 `status: consumed`;weekly-26 的 W26 weekly 檔尚未產出、且內含多項待 Francis 拍板 → 標 `status: pending`(或 `open`),別一律標 consumed,否則看不出哪份還沒落地。

- **`tags` 補成樣板格式**:daily-25 補 `tags: [system, meta, maintenance-proposal, daily]`;weekly-26 把末項換成 `weekly` → `tags: [system, meta, maintenance-proposal, weekly]`。

## 一個跨檔提醒

- **weekly-26 在 Inbox 仍標 `pending`,等於提醒「W26 weekly 檔尚未寫」**:它本身的提案(把發布外洩 BLOCKER 升上 Now.md、`_控管/` 收斂)都還沒落地。移檔時建議在其 frontmatter 留 `status: pending`,當成「W27 收束前必須消化」的待辦旗標,而非靜默歸檔了事。

要我針對某一則草擬「移檔後的完整 frontmatter 區塊」純文字版嗎?仍唯讀、不寫檔。


## 發布快檢 (`publish-quick`)

快檢完成。掃了全 vault 的 `publish:` 旗標、客戶/私密資料夾、以及正典入口檔的 `[[連結]]`。以下為唯讀提案,未動任何檔案:

## publish 旗標(over-exposure 風險)

- **`publish:true` 目前只有 `index.md` 一頁,且內容是安全的 allowlist 首頁** — 它明寫「預設不公開、僅少數頁對外」,本身沒洩漏。單看旗標是乾淨的,**沒有客戶/私密頁誤標 `publish:true`**。

- **⚠️ 真正風險是「旗標根本沒在守門」** — 全 vault 只有 3 個檔帶 `publish:` 鍵(`index.md=true`、`n8n-stack-ROI=false`、`INTEGRATION_PLAN=false`),其餘**數百檔完全無 `publish:` 鍵**。這正是 daily blocker 揭的問題:發布走 rsync 排除式 blocklist,不看 `publish:`。**若 Quartz 端是 RemoveDrafts(預設)而非 ExplicitPublish,沒標 `publish:false` 的全部會上線**。提案:把「確認 Quartz = ExplicitPublish」列為今日 daily 最高優先,owner=Francis,未確認前視同紅燈。

## 該補 `publish:false` 的敏感區(defense-in-depth)

- **`03 公司/`(財務/合約/登記/商標)** — 目前僅 `README.md`,但定位就是公司機密檔區,**無任何 `publish:` 防護**。提案:於 `03 公司/README.md` 補 `publish: false`,並在該資料夾建 folder-note 統一壓旗標。

- **`01 專案/寶鋪 showcase/`(26 檔,客戶交付物)** — 客戶端產物,不該進公開站,目前無 `publish:false`。提案:整個資料夾預設 `publish:false`(folder-level),逐檔 opt-in 才公開。

- **`04 Github/VistwinProject/`(B-Table、F-wall、Escape-from-Crisis 等內部專案規格)** — 屬內部 IP/規格,無 publish 防護。提案同上補 `publish:false`。`01 專案/_控管/`(10 個 PM 控管物件)亦同。

## 壞連結

- **正典入口檔的 `[[連結]]` 全數解析正常** — `index.md`(唯一公開頁)無 wikilink;`Now.md` / `CLAUDE.md` 的 8 條連結(Entity_Map、三個 SKILL、W26 daily/README、Repos inventory)逐一存在,**對外可見路徑上沒有明顯壞連結**。健檢報告的 243 孤兒連結集中在內部檔,不影響發布面,可降為內務、非發布紅燈。

要我針對某個敏感資料夾草擬「folder-note + `publish:false` frontmatter」的純文字版,或列出全 vault 壞連結完整清單(需放行一次 python/comm 掃描指令)嗎?仍維持唯讀、不寫檔。



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/35?signature=9b884c7757dd89936a31354af509dc25161441de700f28bfbf8eb58dcfb21de6
