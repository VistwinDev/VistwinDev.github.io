---
title: 維護提案 daily 2026-06-25
type: maintenance-proposal
---

# 🔍 三庫維護提案 — daily 2026-06-25

## daily 日誌 (`daily-journal`)

已讀完今天 daily、Now.md,並核對了今天實際產生的檔案。今天的 daily 主體是「回補 6/22–6/24」,但**今天(6/25)本身發生的事**記錄不全。以下為今天 daily 該補的具體提案(只提案,未寫檔):

**進度(今天實際產出,daily 未記)**
- **補一條「策略討論落檔」**:今天新建 `98 資料/Strategy/2026-06-25_n8n-stack-ROI.md`(n8n 自動化堆疊策略/ROI,`publish:false`、owner Francis、P2),daily 完全沒提。建議在「完成項」加一條,並標其延伸動作「做成 VISION BASE 簡報(PPT)」,免得這個 todo 飄在檔案 frontmatter 裡跨日遺失。

**決策**
- **把 n8n 定位寫進決策段**:該策略檔已得出明確結論(「n8n 當薄殼編排 + Claude 當大腦」、利基不會被巨頭收割)。這是一條真決策,值得從冷藏的 Strategy 檔提一句到 daily「決策」段,與既有的「cron 降 W27」並列,讓自動化路線的判斷集中可見。

**blocker / 待釐清**
- **回填「心情/能量」收尾欄**:第 47–49 行三格仍是空模板(`/10`、待填),今天若要封板需本人填;建議提案提醒,否則這天 daily 形同未收尾。
- **blocker 加「下一步動作人」**:現有兩個 blocker(ExplicitPublish 驗證、DASHBOARD_PAT)責任都歸 Francis 且需開 site repo,但沒有可執行的最小下一步。建議補一句具體動作(例:「開 site repo 搜 `quartz.config` 確認 emitter 是 ExplicitPublish」),讓 blocker 可被推進而非只標記。

**一致性(daily ↔ Now.md)**
- **同步 cron 定調到 Now.md**:今天 daily 已定調「cron 降 W27、先手動跑兩天」,但 `Now.md` 第 22、30 行仍寫「cron 待 Dispatch 派出/近期要啟動」。建議今天 daily 收尾時順手提示 Now.md 該對齊,否則三庫又出現跨檔不一致(正是健檢「正典脫節」失分項)。

要不要我針對其中某一條,給出可直接貼進 daily 的草稿文字?(仍不寫檔,只給文字)


## Inbox 分類 (`inbox-triage`)

已讀完 Inbox。結論與提案如下:

**現況**
- `00 系統/Inbox/` 目前**只有 `.gitkeep`,沒有任何待歸檔筆記** — 沒有可歸類的零散筆記。根目錄的 4 份 `.md`(`_changelog`、`index`、`Now`、`CLAUDE`)都是常駐正典,不屬 inbox 範疇。

**提案(本次 inbox-triage 無物可搬,以下為機制面建議,皆不寫檔)**
- **本次 triage 標記為 0 項,正常收尾**:Inbox 空是好狀態,不需強行製造歸檔動作;daily 若要記一句即「inbox 已清空 / 無待處理」。
- **回頭看 6/24 的 inbox-triage commit**(`8d42f0e maint(daily): inbox-triage 2026-06-24`):Inbox 現在空,代表昨天那批可能已歸檔或當時也是空。建議下次提案前先 `git log` 對一下,確認「空」是因為已處理而非筆記從未進到 Inbox(捕捉漏接的 capture 管道問題)。
- **可疑的 capture 缺口**:今天明明產生了 `98 資料/Strategy/2026-06-25_n8n-stack-ROI.md` 這類新想法,卻**沒先經過 Inbox**就直接落到正式資料夾。若這是常態,代表 Inbox 在實際工作流裡被繞過 → inbox-triage 這個維護環節會長期空轉。建議下次與 Francis 確認:新筆記是否該一律先進 Inbox 再 triage,還是 Inbox 機制其實已名存實亡、該從三庫維護流程移除。

要不要我幫你檢查「為什麼新筆記沒進 Inbox」的捕捉路徑(例如 Templates / daily skill 是否有把 capture 導向 Inbox 的設定)?仍只提案、不寫檔。


## 發布快檢 (`publish-quick`)

已完成快檢。核心結論很清楚,提案如下(只提案,不寫檔):

**發布旗標現況 — 風險不在「誤標 true」,在「根本沒在用旗標」**
- **全庫只有 3 個檔用 `publish:` 旗標**(`index.md: true`、`n8n-stack-ROI` 與另一檔 `false`)。**所有寶舖 showcase、03 公司、_控管、visionbase 檔案都沒有 publish 旗標**。在 allowlist(ExplicitPublish,只 emit `publish:true`)下這些是安全的;但若 site 端是 RemoveDrafts(只擋 `draft:true`),**這幾十個客戶/私密檔會全部上線** — 這正是昨日 daily blocker 的實體清單。
- **`index.md` 的宣告與實機制可能不符**:`index.md` 第 13 行寫「本站採 allowlist 發佈,預設不公開」,但這只是文案,**vault 端的 rsync 是排除式 blocklist、不看 `publish:` 旗標**(見昨日 blocker)。建議把這條列為今日驗證重點:在 site repo 確認 Quartz emitter 是 `ExplicitPublish` 而非 `RemoveDrafts`,否則 index.md 的承諾是空的。

**最該優先處理的明確高敏資料**
- **`03 公司/`(財務/合約)應加防護旗標**:該資料夾檔案既無 `publish:` 旗標、又屬最敏感類。提案:對 `03 公司/` 與 `01 專案/_控管/` 批次補 `publish: false`(或 `draft: true`),做成**不論 site 端用哪種 emitter 都安全**的雙保險,而非賭 site 設定正確。
- **寶舖 showcase 是客戶外包內容**:同樣零旗標。若短期無法驗 site 端,建議至少對 `寶鋪 showcase/` 加 `publish: false`,把「會不會外洩」從不確定變確定。

**壞連結**
- **發布表面 `index.md` 無 wikilink,乾淨**;但健檢已報 **243 孤兒連結**(見昨日 daily 第 16 行),這是全庫等級問題,非今日 daily 快檢能逐條清。提案:今日不展開,僅在 daily 記一句「壞連結待 weekly(W26 收束)用 `vault-health.mjs` 專項處理」,避免跨日累積又無歸屬。

**收尾建議(本次快檢的一句話結論)**
- 今日發布健康真正的單一行動項 =「**確認 site repo emitter 類型**」。其餘(補旗標、清孤兒連結)都依賴這個答案才知道是否緊急。建議把它從 blocker 升為今日 daily 的**唯一 P0**。

要不要我幫你產出一份「該補 `publish:false` 的檔案清單」(掃 `03 公司/`+`寶鋪 showcase/`+`_控管/`),方便你之後一次補?仍只提案、不寫檔。



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/31?signature=27d4e26c1a8abd23660fd8ec7974404dac933ca06eaba06c4fd338165965e874
