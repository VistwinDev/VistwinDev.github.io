---
title: 維護提案 daily 2026-06-30
type: maintenance-proposal
---

# 🔍 三庫維護提案 — daily 2026-06-30

## daily 日誌 (`daily-journal`)

讀完 `Now.md`、最後實檔 daily（06-25）與 06-29 維護提案。**今天是 2026-06-30(二),W27;最後一篇實檔 daily 仍停在 06-25,06-26→06-29 連四天斷,今天再不補=斷第五天。Now.md / health 也仍停 06-25 / 06-24。** 以下純文字提案,**未動任何檔**:

- **進度・誠實標斷檔到 D+5,別靜默搬待辦**:今天 daily 開頭註明「回補 6/26–6/30」,期間唯一實際產出(06-27/28/29 三份維護提案 + 每日 vault backup commit)記為完成項,其餘一律標空轉。重點是別重演 W22–W25 把待辦原樣搬下一天的假進度。

- **🔴 blocker・節奏重啟已證偽,cron 派出不可再降級**:06-25 訂「手動跑兩天觀察」,結果連斷四天 → 06-29 已下「手動不可續命」。今天 blocker 段把「cron 派出」從 W27「議題」確定為 **W27 必做、且為節奏存活前提**,owner / 車道(n8n Dispatch)寫死,不再每日懸置。

- **🔴 blocker・發布外洩風險滾到 ~6 天仍未升上 Now.md**:rsync 排除式 blocklist 不看 `publish:`,若 site 端 Quartz 是 RemoveDrafts 而非 ExplicitPublish,`03 公司/` 財務合約全上線。daily 明寫 **owner=Francis、動作=開 site repo 確認 emitter 模式;未確認前手動 rsync 視同帶風險**,並把此條(三度被提)一併補進 Now.md「等待/blocked」。

- **進度・carry-over 升 D+5**:06-25「明日要做」三項——showcase 各頁 build-ready 盤點、`_控管/` 定位待 Francis、跟進 site repo ExplicitPublish——四天無一推進,顯式標「carry-over D+5」;第三項與上條 blocker 合併追,不重開新項。

- **🔴 W26 收束三件懸空,今天 daily 須點名為 W27 開週動作**:① W26 weekly 實檔從未寫(只有提案 pending);② 06-26 訂的「收束日重跑 vault-health」未跑,health 停 39/100 @06-24;③ `sprints/` 只到 W26,**今天寫 daily 前需先確認 / 建 W27 資料夾與歸屬**。收尾掛一條「→ 補 W26 收束 + 正式開 W27」。

- **決策待拍板・`_控管/` 雙重真相源收斂列 W27 第一優先**:06-27 weekly 揭出 `_控管/` 物件與 11 篇 `pm:true` 內容筆記並存漂移、`sprint:` 全停 W21、`n8n-stack-ROI` 在冷藏區卻帶 `pm:true`。daily 決策段點名為 W27 首案(建議:內容筆記拔 `pm:`、dashboard 只撈 `01 專案/_控管/`),待 Francis 拍板。

- **進度・雙軌主線各補一行狀態**:主線 A(Physical AI)6/24 lab 盤點後整週未動、主線 B(BIM+DT)整週零動作,各記一行,避免兩軌在無紀錄下被默認支線化(Entity Map 定義 A/B 同級)。

要我接著針對某條(例如「W27 正式開週段」或「節奏證偽 + cron 必做段」)草擬具體 daily 段落純文字嗎?仍維持唯讀、只出文字、不寫檔。


## Inbox 分類 (`inbox-triage`)

讀完 Inbox 5 則零散筆記與正規樣板 `00 系統/Meta/maintenance/maint-daily-2026-06-24.md`(五欄 frontmatter:`title / type / created / status / tags`,檔名用 `maint-` 前綴)。Inbox 現有 5 則,全部只有 `title`+`type` 兩欄、缺 `created/status/tags`。以下純文字歸檔提案,**未動任何檔**:

- **5 則全部同類、同一去向 → `00 系統/Meta/maintenance/`**:`維護提案-daily-2026-06-25`、`維護提案-weekly-2026-06-26`、`維護提案-daily-2026-06-27`、`維護提案-daily-2026-06-28`、`維護提案-daily-2026-06-29` 皆 `type: maintenance-proposal`,與 6/24 正規檔完全同類。Inbox 是暫存區、非終點,移完後回到只剩 `.gitkeep` 的乾淨態。

- **檔名統一改 `maint-` 前綴(對齊 6/24)**:`維護提案-daily-2026-06-25.md` → `maint-daily-2026-06-25.md`、`維護提案-weekly-2026-06-26.md` → `maint-weekly-2026-06-26.md`,其餘 27/28/29 比照,讓 `Meta/maintenance/` 命名一致、可程式化掃描。

- **五則都補齊缺的三欄 `created / status / tags`**:`created` 各依檔名日期填 `2026-06-25`～`2026-06-29`;`title` 可選同步去空格對齊 6/24 的「維護提案 daily YYYY-MM-DD」格式(不強制)。

- **`status` 依「是否已落地實檔」分填,別一律 consumed**:
  - `daily-25` → **`consumed`**(已餵進 `2026-06-25.md` daily 實檔)。
  - `weekly-26` → **`pending`**(W26 weekly 實檔從未產出)。
  - `daily-27` / `daily-28` / `daily-29` → **`pending`**(06-26→06-29 斷檔,內容均未落地成 daily 實檔)。

- **`tags` 補成樣板格式 `[system, meta, maintenance-proposal, <週期>]`**:四則 daily 末項填 `daily`、`weekly-26` 末項填 `weekly`。

- **四則 `pending` 即 W26 收束旗標,移檔務必保留 `status: pending`**:`weekly-26`+`daily-27`+`daily-28`+`daily-29` 標 pending,正好標示「W26 weekly 未寫 + 06-27/28/29 daily 內容未落地」這批待消化事項,別靜默歸成 consumed 而讓斷檔被默認結清。

- **不建議現在加子資料夾結構**:`maint-` 前綴 + ISO 日期已足夠排序,目前量小(連 6/24 共 6 檔),`Meta/maintenance/` 維持平鋪即可,避免過度結構化。

要我針對某一則草擬「移檔後完整五欄 frontmatter 區塊」純文字版嗎?仍維持唯讀、只出文字、不寫檔。


## 發布快檢 (`publish-quick`)

發布快檢完成。今天(06-30)重掃全 vault `publish:` 旗標、四個客戶/私密區、與正典入口檔連結。純文字提案,**未動任何檔**:

- **`publish:true` 全 vault 仍只有 `index.md` 一頁,且完全乾淨**:實測 `index.md` 內文 `[[wikilink]]` 數 = 0(「內部首頁 → Vault Home(私有,不公開)」是純文字),不會把內部頁帶上線。**沒有任何客戶/私密頁誤標 `publish:true` — 誤公開風險:零。** 其餘 grep 命中全是維護提案內文「引述」`publish:` 字串,非 frontmatter 旗標。

- **真旗標總數仍是 3 個,旗標根本沒在守門(跨日 P0,沿用昨日 blocker、不重開新項)**:全 vault 僅 `index.md=true`、`98 資料/Strategy/…n8n-stack-ROI.md=false`、`02 技術/Visustwin/INTEGRATION_PLAN.md=false` 三鍵,其餘數百檔無 `publish:` 鍵。發布走 rsync 排除式 blocklist、不看旗標 → **若 site 端 Quartz 是 RemoveDrafts(預設)而非 ExplicitPublish,所有無旗標檔全部上線**。owner=Francis,動作=開 site repo 確認 emitter 模式。

- **四個敏感區實測存在、且零 `publish:` 防護(被動安全)**:`03 公司/`(登記/商標/財務/合約)、`01 專案/寶鋪 showcase/`(客戶交付)、`01 專案/_控管/`(PM 控管)、`04 Github/`(內部規格)皆確認存在,實測 frontmatter `publish:false` 命中數 = 0。目前安全只靠「沒被標 true」被動成立、非主動擋。**提案 defense-in-depth:對這四區批次補 `publish:false`(或 folder-note 壓旗標),使不論 emitter 是哪種都安全** — 把外洩從不確定變確定的最低成本動作。

- **`index.md` 文案與實機制仍不符**:第 9 行宣告「本站採 allowlist 發佈」,但 vault 端 rsync 實際不按旗標過濾。**在解掉 emitter blocker 前,別擴大 `publish:true` 使用、也別在未驗證下 flip 任何 showcase 頁為 true**,以免文案承諾落空。

- **發布面連結乾淨**:唯一公開頁 `index.md` 內無任何 `[[連結]]`(實測 0),發布面不存在壞連結或反向帶出私密頁的風險。內部孤兒連結(健檢 243 條)集中在內部檔、不在發布面,維持「內務待清」即可,不列今日發布紅燈。

- **預防提醒・showcase 日後對外要先查連結外溢**:`01 專案/寶鋪 showcase/` 若日後 flip `publish:true`,務必先確認頁內 `[[連結]]` 不會把 `_控管/`、財務、合約等私密頁反向帶上線(交付物常反連回內部 PM 物件)。

要我產出「該補 `publish:false` 的四區檔案完整清單」或草擬某資料夾的 folder-note + `publish:false` 純文字版嗎?仍維持唯讀、不寫檔。



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/41?signature=ef4ccf32c35540f80397d2e84e0972c1f60b12037ae1f677eb7d0dfaeda3916e
