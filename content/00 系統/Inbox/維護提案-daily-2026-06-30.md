---
title: 維護提案 daily 2026-06-30
type: maintenance-proposal
---

# 🔍 三庫維護提案 — daily 2026-06-30

## daily 日誌 (`daily-journal`)

讀完 `Now.md`、W26 最後實檔 daily（停在 06-25）與今日 git log。現況確認：今天是 **W27**、最後 daily 實檔停在 **06-25**（06-26→06-30 連 5 天斷檔）、今日無 code commit（僅 vault backup ×12）、`sprints/2026-W27/` 尚未建、W26 weekly 從未寫成實檔、health 仍 **39/100 @06-24**。

今天 daily 該補的具體提案（唯讀，未寫任何檔）：

- **誠實標斷檔 D+5，別搬假進度**：開頭註明「回補 6/26–6/30」。期間唯一真實產出＝四份維護提案（06-27/28/29/30）＋每日 vault backup，記為完成項；其餘誠實標空轉，不重演 W22–W25 假進度。

- **🔴 節奏證偽 → cron 派出升為「存活前提」**：06-25 訂「手動跑兩天觀察」結果連斷四天，「手動可續命」已被證偽。把 daily/weekly 兩個 cron（n8n Dispatch）寫死為 **W27 必做**，固定 owner／車道，不再每日懸置。

- **🔴 發布外洩 blocker 滾到 D+5 仍未上 Now.md**：rsync 走排除式 blocklist 不看 `publish:`，site 端 Quartz 若為 RemoveDrafts，`03 公司/` 財務合約恐全上線。daily 明寫 **owner=Francis、動作=開 site repo 確認 emitter 模式；未確認前手動 rsync 視同帶風險**，並同步補進 `Now.md` 的「等待／blocked」（已三度被提仍未上 Now）。

- **🔴 開週動作・W27 資料夾尚未建**：`sprints/` 只到 W26。寫 daily 前先確認／建 `2026-W27/README`。同時點名 W26 收束兩件未結：① W26 weekly 實檔從未寫；② 06-26 訂的「收束日重跑 vault-health」未跑，health 仍停 **39/100 @06-24**。

- **決策待拍板・`_控管/` 定位列 W27 首案**：`_控管/` 與 Entity Map 主線 A/B 對應、是否取代舊 sprint 流，自 06-24 懸置至今 → 標「待 Francis 拍板」，別繼續跨日累積。

- **進度・雙軌主線各補一行**：主線 A（Physical AI）6/24 lab 盤點後整週未動、主線 B（BIM+DT）整週零動作，各記一行，避免兩軌在無紀錄下被默認支線化（Entity Map 定義 A/B 同級）。

- **carry-over・06-25「明日要做」三項升 D+5**：showcase 各頁 build-ready 盤點、`_控管/` 待 Francis、site repo ExplicitPublish 跟進 — 四天無一推進，顯式標「carry-over D+5」，第三項併入上方發布 blocker，不重開新項。

要我把其中任一條（例如「W27 正式開週段」或「節奏證偽＋cron 必做段」）草擬成可直接貼入 daily 的純文字段落嗎？仍維持唯讀、不寫檔。


## Inbox 分類 (`inbox-triage`)

讀完 Inbox 全部 6 則零散筆記＋正規範本 `Meta/maintenance/maint-daily-2026-06-24.md`。現況確認：6 則皆 `type: maintenance-proposal`、**只有 `title`+`type` 兩欄**（缺 `created`/`status`/`tags`）、檔名用 `維護提案-` 前綴而非正規 `maint-`、全平鋪在 Inbox。

歸檔提案如下（唯讀，未動任何檔）：

- **6 則全部同類、同一去向 → `00 系統/Meta/maintenance/`**：皆 `maintenance-proposal`，與 6/24 正規檔完全同類。Inbox 是暫存區非終點，清空後回到只剩 `.gitkeep` 的乾淨態。

- **檔名統一改 `maint-` 前綴**：`維護提案-daily-2026-06-25.md` → `maint-daily-2026-06-25.md`、`維護提案-weekly-2026-06-26.md` → `maint-weekly-2026-06-26.md`，27/28/29/30 比照，讓 `Meta/maintenance/` 命名一致、可程式化掃描。

- **6 則補齊缺的三欄 `created / status / tags`**：`created` 各依檔名日期填（`2026-06-25`～`2026-06-30`）；`title` 已是「維護提案 daily YYYY-MM-DD」格式、與 6/24 對齊，可不動。

- **`status` 依「是否已落地實檔」分填，別一律 consumed**：
  - `daily-25` → **`consumed`**（已餵進 `2026-W26/2026-06-25.md` daily 實檔）。
  - `weekly-26` + `daily-27/28/29/30` → **`pending`**（06-26→06-30 連 5 天斷檔＋W26 weekly 從未寫成實檔，內容均未落地）。

- **`tags` 補成樣板格式 `[system, meta, maintenance-proposal, <週期>]`**：五則 daily 末項填 `daily`、`weekly-26` 末項填 `weekly`。

- **5 則 `pending` 是斷檔／W26 收束的旗標，移檔務必保留**：這批正好標示「W26 weekly 未寫＋06-27~30 daily 內容未落地」的待消化事項，別靜默歸成 consumed 而讓斷檔被默認結清。

- **`daily-2026-06-30` 含未完成核准連結，移檔保留原文**：末尾有 `localhost:5678/form-waiting/50` 核准表單 URL；歸 `pending` 時保留原文，不必清理（屬待核准狀態的一部分）。

- **暫不加子資料夾**：`maint-` 前綴＋ISO 日期已足夠排序，量小（連 6/24 共 7 檔），`Meta/maintenance/` 維持平鋪即可。

要我針對某一則草擬「移檔後完整五欄 frontmatter 區塊」純文字版嗎？仍維持唯讀、不寫檔。


## 發布快檢 (`publish-quick`)

發布快檢完成。重掃全 vault `publish:` 旗標、**兩個** `index.md`、四個客戶/私密區的防護狀態與發布面連結。本次有一項**前幾日提案漏掉的新發現**。提案如下（唯讀，未動任何檔）：

- **誤公開風險仍為零，但「唯一公開頁無連結」的舊結論不完整**：`publish:true` 全 vault 只有根 `index.md` 一頁（內文確無 `[[連結]]`、Vault Home 是純文字）。但 **`00 系統/index.md` 是第二個 `index.md`，前幾日提案都沒點到它**——它無 `publish:` 鍵（ExplicitPublish 下安全），但**滿是指向正典的 `[[wikilink]]`**（`[[Now]]`、`[[Entity_Map]]`、`[[Vault Home]]`、working-with-frncs 等）。**若 site 端是 RemoveDrafts（全鏡像），這頁會連同它的內部連結一起上線**，等於把正典導覽圖對外曝光。建議今日 daily 把它跟敏感四區一起列入 defense-in-depth 對象。

- **真旗標總數仍只有 3 個，旗標根本沒在守門（跨日 P0，沿用 blocker、不重開新項）**：全 vault 僅 `index.md=true`、`98 資料/Strategy/…n8n-stack-ROI.md=false`、`02 技術/Visustwin/INTEGRATION_PLAN.md=false` 三鍵，其餘數百檔無 `publish:` 鍵。發布走 rsync 排除式 blocklist、不看旗標 → **site 端 Quartz 若是 RemoveDrafts（預設），所有無旗標檔全部上線**。owner=Francis，動作=開 site repo 確認 emitter 模式。此 blocker 已滾至 D+5 仍未上 `Now.md`。

- **四個敏感區實測在位、零 `publish:` 防護（被動安全）**：`03 公司/`（登記/商標/財務/合約）、`01 專案/寶鋪 showcase/`、`01 專案/_控管/`、`04 Github/` 皆確認存在，`publish:false` 命中＝**0**。安全只靠「沒被標 true」被動成立。**提案 defense-in-depth：對這四區＋`00 系統/index.md` 批次補 `publish:false`（或 folder-note 壓旗標），使不論 emitter 哪種都安全。**

- **發布面連結乾淨、但內部導覽頁連結需在改 emitter 時一併查**：根 `index.md`（唯一公開頁）內 wikilink＝0，發布面無壞連結。抽查 `00 系統/index.md` 的連結目標 `[[Vault Home]]`、`[[Entity_Map]]` **實檔皆存在、非壞連結**；風險不在「壞」而在「一旦全鏡像就外溢」。健檢 ~243 條孤兒連結集中在內部檔、不在發布面，維持「內務待清」即可，不列今日發布紅燈。

- **`index.md` 文案與實機制不符**：根 `index.md` 宣告「本站採 allowlist 發佈」，但 vault 端 rsync 實際不按旗標過濾。**解掉 emitter blocker 前，別擴大 `publish:true`、也別在未驗證下 flip 任何 showcase 頁為 true**，以免文案承諾落空。

- **預防提醒・showcase 日後對外要先查連結外溢**：`01 專案/寶鋪 showcase/` 若日後 flip `publish:true`，務必先確認頁內 `[[連結]]` 不會把 `_控管/`、財務、合約等私密頁反向帶上線（交付物常反連回內部 PM 物件）。

要我產出「該補 `publish:false` 的四區＋`00 系統/index.md` 完整清單」，或草擬某資料夾的 folder-note + `publish:false` 純文字版嗎?仍維持唯讀、不寫檔。



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/95?signature=93cb22df9cefee1cfd696c415ed4477c8f9589c51b0ebf742064871e0ae26344
