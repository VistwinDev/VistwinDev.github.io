---
title: 維護提案 daily 2026-06-24
type: maintenance-proposal
---

# 🔍 三庫維護提案 — daily 2026-06-24

## daily 日誌 (`daily-journal`)

讀完今天的 daily（已回補 6/22–6/24）與 Now.md。daily 框架已相當完整,以下是針對「今天 6/25 實際動作」與既有缺口的補寫提案:

- **補「今日 daily 節奏重啟」這件事本身就是進度** — 今天跑了 publish-quick 與三庫維護(daily),這是 35 天斷檔後第二天連續執行。建議在「完成項」加一條:6/25 daily 流程實際手動跑通(對應「在做項」那條 cron 未派出的補償動作)。

- **「在做項」的 cron 待決事項,今天應收斂成決策** — 文中三處重複提到「daily/weekly cron 是否手動補跑/是否走 n8n」(在做項 + 明日要做 + Now.md)。建議今天直接記一個決策:**先以手動 skill 跑兩天觀察,cron 派出降為下週議題**,免得它一直懸著當每日待辦。

- **回填「完成項」裡兩個明確 TODO 的當前答案** — 兩條完成項都掛了「待補/待釐清」:① 寶舖 showcase 哪幾頁已 build-ready;② `_控管/` 層為何另立、與 Entity Map 主線 A/B 的對應、是否取代舊 sprint 流。這兩個是結構性決策,建議今天至少先記下「現況一句話」,不要讓它跨日累積。

- **新增 blocker:publish-quick 的 allowlist 守門無法在本 vault 驗證** — daily 已點出「rsync 是排除式 blocklist、不看 publish: 旗標」「需確認 site Quartz 用 ExplicitPublish 而非 RemoveDrafts」。這是真正會洩漏 `03 公司/` 財務合約的風險,建議升級為**明確 blocker 並標責任歸屬(需開 site repo 驗)**,而非埋在 blocked 段的說明裡。

- **兩個逾期舊 blocker 今天就該下處置,而非再排到明日** — `DASHBOARD_PAT` 與 org rename(逾 5 週)在「明日要做」第一條。建議今天直接在 blocker 段標註:org rename **降級為 backlog / 重訂期限**,DASHBOARD_PAT **標明是否仍要自動 sync 或永久走手動 rsync** — 給結論,別再滾到明天。

- **健檢 39/100 應有可量測的今日對照** — 完成項提到健檢三大失分(正典脫節/節奏斷/243 孤兒連結)。建議記一條:今天 daily 連續執行已直接改善「節奏斷」一項,可附「下次健檢預計重跑日」當追蹤錨點。

- **心情/能量/一句話三欄仍是空白佔位** — 這是 daily 的固定收尾欄位,提醒今天填,否則模板形同未完成。

要不要我接著把其中某幾條整理成可直接貼進 daily 的文字?(仍不會寫檔,除非你指示)


## Inbox 分類 (`inbox-triage`)

Inbox 內只有一篇 `維護提案-daily-2026-06-24.md`,而它本身不是外部隨手記、而是「三庫維護自己的產出物」被丟回 Inbox。以下為唯讀提案:

- **核心判斷:這篇不該留在 Inbox。** Inbox 應只放「未處理的外部零散輸入」,維護報告佔位會讓 Inbox 永遠清不空——這正是健檢顯示「Inbox 待處理 0」卻與現實矛盾的根因。它需要被「歸檔」而非「待處理」。

- **該歸到哪:`00 系統/Meta/maintenance/`(需新建,目前不存在)。** 與既有的 `Meta/health/` 同層,讓「健檢報告 + 維護提案」兩類自動產出物集中、可一條 glob 撈出。若不想新建層,退而求其次放 `00 系統/Meta/` 根,但會與 `health/` 慣例不對稱。

- **檔名對齊 health 慣例:改成 `maint-daily-2026-06-24.md`。** health 是 `health-2026-06-24.md` + `latest.md`;建議比照在 `maintenance/` 也建一個 `latest.md` 指標,方便「最近一次維護」快速定位。

- **frontmatter 該補(現只有 `title` + `type`):**
  - `created: 2026-06-24` — 對齊 health 的 `created` 欄。
  - `tags: [system, meta, maintenance-proposal, daily]` — 否則無法被健檢/查詢索引到。
  - `status: consumed` — 對照今日 daily(`2026-06-25.md`),這篇第 12–28 行的提案多數已落進今日 daily,且它寫「今天 daily 還沒建」但今天已建好,**內容已過期**;不標狀態日後會被誤當待辦重跑。
  - 保留 `type: maintenance-proposal`(這欄選得好,建議定為此類產出的標準 type)。

- **搬檔時順手查一件事:第 86 行 `localhost:5678` 核准表單連結。** 這篇若移出/改名,該 n8n form webhook 可能變死連結;搬檔時確認此提案是否已透過該表單核准結案,結案則移除連結或標 `已失效`。

- **流程根因(非單檔):改規則讓三庫維護的提案預設寫到 `Meta/maintenance/`,別再寫進 Inbox。** 不改規則,下一輪維護又會在 Inbox 生一篇,Inbox 職責永遠不乾淨。

- **需求釐清:扣掉這篇產出物,Inbox 沒有任何真正的隨手記。** 若你近期有桌面/手機隨手記,可能落在 vault 外(Drive / Gmail 草稿 / Apple Notes)。要的話下一輪我可去 Drive/Gmail 撈,再提案歸檔點。

最關鍵一條:**移出 Inbox → 改名 `maint-daily-2026-06-24` → 補 frontmatter(含 `status: consumed`)**,三動作一次到位,Inbox 才真正歸零。依指示僅提案,未動任何檔。


## 發布快檢 (`publish-quick`)

已掃全庫 `publish:` 旗標、客戶/私密區、以及 `技術/VisTwin/` 壞連結。結論:**無誤公開風險,壞連結是大宗**。以下為唯讀提案:

- **誤公開風險 — 無(維持現狀)。** 全庫唯一 `publish: true` 是根目錄 `index.md`,內容乾淨:只有標題 + allowlist 說明,「內部首頁 → Vault Home(私有,不公開)」是純文字、**無任何 `[[連結]]`**,不會把內部頁連帶上線。allowlist 模型成立。

- **客戶/私密區零 publish 旗標(安全)。** `01 專案/`(含寶鋪 showcase / welltek)、`03 公司/`(登記/商標/財務/合約)全區掃過,**沒有任何 `publish:` 旗標**,無誤標公開。另兩個 `publish:` 都是 `false`:`98 資料/Strategy/2026-06-25_n8n-stack-ROI.md`(正確標不公開)、`02 技術/Visustwin/INTEGRATION_PLAN.md`(內文 publish 是 pub/sub 術語,無關發布)。

- **壞連結 — 大宗、最高槓桿一條:`技術/VisTwin/`(少一個 `us`)應為 `技術/Visustwin/`。** 已確認 `02 技術/VisTwin/` 資料夾**不存在**,全是孤兒連結,散在 **37 個檔**(近 25 個 Plugin README、`Backlog`、`sprints/README`、`GitHub Overview`,連 `01 專案/visionbase/README` 與 `寶鋪 showcase/welltek/README` 也中)。建議全庫批次取代 `技術/VisTwin/` → `技術/Visustwin/`,一次清掉這批(對應健檢「243 孤兒連結」最大可一鍵修區塊)。

- **批次取代必須排除這 4 類「引述用」檔,改了反而失真:** `99 封存/Metaarchetech-site/README.md`(legacy 凍結區)、`00 系統/Meta/health/health-2026-06-24.md` 與 `latest.md`、`00 系統/Inbox/維護提案-daily-2026-06-24.md` — 它們是在**引述**這個壞字串,不是真要連過去。建議批次範圍限定 `02 技術/`、`01 專案/`、`00 系統/GitHub Overview.md`。

- **模板兩檔需逐條確認:** `Templates/Sprint Planning.md`、`Sprint Retro.md` 也在清單內,批次時只改 `VisTwin→Visustwin`、**保留任何 `{{...}}` Templater 佔位符**,別誤傷。

- **唯一需查證的真風險(非本 vault 可驗):rsync 是否真按 `publish:` 過濾。** 本 vault 根無 quartz/全域發布設定,理論上只 `index.md` 出去;但實際 sync 走手動 rsync(`-av --delete` + 排除式 blocklist,**不看 `publish:` 旗標**)。守門其實落在 site repo 的 Quartz 設定 — **若用 RemoveDrafts 而非 ExplicitPublish,allowlist 形同虛設**。本 vault 無法驗,建議列待辦去 site 端確認。

- **預防提醒:showcase 對外前先過 guard。** 寶鋪 showcase 6/22–24 大改,日後若 flip `publish: true`,務必先檢查頁內 `[[連結]]` 不會把 `_控管/`、財務、合約等私密頁帶上線。目前還沒到這步,但很可能很快碰到。

最高槓桿:**批次修 `VisTwin→Visustwin`(37 檔,排除封存區與健檢/提案引述的 4 檔)**;最該查證:**rsync 是否真按 publish 過濾**。依指示僅提案,未動任何檔。



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/22?signature=8467756ef1d604872c49be3ac890e4a15818c7785a71108764406ed4745baddf
