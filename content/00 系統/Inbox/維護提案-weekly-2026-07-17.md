---
title: 維護提案 weekly 2026-07-17
type: maintenance-proposal
---

# 🔍 三庫維護提案 — weekly 2026-07-17

## weekly 總結+Now (`weekly-summary`)

本週 daily(07-12～07-17)已讀完:07-12/13/15 三份整包 502 空殼,只有 07-14/16/17 有實質內容,且三天講的幾乎是同一批未落地事項。以下為 weekly 提案(**唯讀,未寫任何檔**):

## Weekly 總結提案(W28–W29,2026-07-11～07-17)

- **🔴 本週最大訊號:daily 節奏形同未啟動。** 最後一篇「有效」daily 仍停在 06-25,本週 6 天裡有效產出只有 3 份「提案」、且無一核准落地 —— 等於連續第 6 輪空轉。weekly 應如實記「節奏斷 ~22 天」,不要記成「本週有 daily」。

- **🟢 唯一真實專案進度 = 寶鋪七區進場施工,但至今只活在提案裡。** 客戶端已從「線上迭代」跨到現場埋線施工底稿(A~G 七區走線圖 + 走線統整,含 mermaid 埋線、MQTT 匯流、埠口分配;07-06 已與擷果設計開現場溝通)。這是里程碑級轉折,卻連續 3 天沒入帳。**建議 weekly 正式記這筆,並據此改寫 Now.md 客戶端段。**

- **🟠 維護 pipeline 定調(拖了整週的待拍板)。** 本週 502↔通交替(07-13 掛/14 通/15 掛/16 通/17 通),成功率約 1/2 且不穩。weekly 應給結論:**降級為「手動節奏為主、自動提案為輔」**,停止每天賭 endpoint 通不通;5 份 502 空殼(07-08/12/13/15 + weekly 07-10)標 `failed` 保留 infra 追蹤。

- **🔴 兩個逾期 blocker 本週持續擴大、都非本 vault 可解。** ①rsync 發布走 `--delete`+排除式 blocklist、**不看 `publish:` 旗標**,逾期 ~22 天;而這期間新增的七區走線(場地/埠口/設備歸屬)、印廠下單(含個人姓名)敏感面持續變大 —— 須在 site repo(owner: Francis)核對 exclude。②GitHub org / vault 資料夾 rename 未動,一動就會集體斷掉 `[[02 技術/…/sprints/…]]` 全路徑連結(07-14 已實掃出 W18/W21 兩處真實斷點)。

- **📂 Inbox 衛生累積成債。** 14 份 daily + 2 份 weekly 提案全堆在 Inbox 未歸檔;07-14 起就提「搬 `Meta/maintenance/` + 補 frontmatter(consumed/failed 分類)+ 補 README」,連提 3 天未核准。建議本週一次清掉,別讓 triage 區繼續當墳場。

## Now.md 更新提案

- **改「最後更新」與本週焦點區塊:** 現寫 `2026-06-25 / Sprint W26`,已脫節 22 天。應改為 W29,焦點主軸換成**寶鋪七區進場施工**;並補建連缺的 W27/W28/W29 sprint 資料夾(至今未開)。

- **改「客戶端(非主線)」段:** 現寫「透過設計公司持續迭代」已過時,應改為「已進入七區進場施工/現場埋線底稿階段,BOM 以 **0609 報價為準**(vault 內 0513 舊版已部分被覆寫,勿照舊下單)」。

- **「等待 / blocked」段兩條各補逾期天數:** rsync publish blocker(~22 天)頂到最上;GitHub org rename 補註「執行前須先掃全路徑連結、改短連結或建全庫 resolver 基線」。

—— 以上全為提案,**未寫入或搬移任何檔案**。要我把 weekly 總結或 Now.md 改版實際落檔,再明講。


## 看板對帳 (`pm-reconcile`)

三處資料已對帳完畢:9 個控管物件(`_控管/*`)、9 份帶 `pm:` 的內容筆記、Now.md。以下為不一致與修正提案(對齊 `_控管/_schema.md`,**唯讀,未寫任何檔**):

- **🔴 內容筆記全數缺 `type`(schema 必填),部分缺 `status` → 灌進 dashboard 會是壞物件。** 9 份 content note 都掛 `pm: true` 卻無 `type`(project/task/milestone);其中 `Physical_AI/README.md`、`visionbase/*`、`寶鋪 showcase/*` 也沒 `status`(或只在頂部另寫一組舊 key)。**提案:content note 要嘛補齊 `type`+`status`,要嘛依 schema 原意「內容筆記不帶 pm、只由 `_控管/*` 用 `ref` 連回」——二擇一,別讓同一物件在兩處半殘並存。**

- **🔴 `status: active`(舊詞彙)不在 schema enum 內。** `Physical_AI/README.md` 頂部 `status: active`、`priority: P0 (5-10 yr)` 用的是自由字串,schema 只認 `todo/in-progress/blocked/done` 與 `P0–P3`;且同檔 pm 區塊又沒 status → 對帳時會與控管檔 `Physical_AI.md`(`in-progress`)打架。**提案:刪頂部舊 key,只留控管檔那一份為準。**

- **🟠 「寶鋪」同一專案被表述成 5 個 pm 物件且 progress 打架。** `_控管/寶鋪.md`=75、`README.md`=75、`build-status.md`=**60**、`0513`=100、`交接`=100 全掛 `pm:true`;里程碑 `_控管/寶鋪·6-8交付.md` 又與 `交接_260608交付速覽.md` 重複表述同一交付。**提案:控管檔為唯一真相源,`build-status` 的 60 若是「實作進度」應改欄名或降為非 pm;交接筆記去 pm、改由里程碑 `ref` 指向。**

- **🟠 sprint 欄三處各說各話(W21 / W26 / W29)。** 全部控管檔與內容筆記 `sprint: 2026-W21`,Now.md 本週焦點寫 `W26`,而今天已是 **W29**。三處無一對上。**提案:統一刷成當期 sprint(W29),或改用 milestone/`due` 驅動、廢掉手動 sprint 欄以免每週漂移。**

- **🟠 多個 `end`/`due` 已逾期但 status 未動。** `寶鋪` end `2026-06-30`、`寶鋪·B-Table` end `2026-06-28`、`Linux_Lab_Plan` due `2026-07-15` 皆已過(今日 07-18),狀態仍 `in-progress`。**提案:逾期項要嘛改 `done`、要嘛展延 `end`;尤其寶鋪實況已進「七區進場施工」(見本週 daily),控管檔卻停在 6/30 交付、progress 75,應更新真實進度與新 end。**

- **🟡 Now.md 漏掉兩個 control 內 active 專案。** 控管有 `visionbase`(in-progress, P1, 35%)與 `vistwin-pm`(in-progress, P1, 50%)兩個進行中物件,Now.md「進行中」段完全沒提;反之 Now.md「客戶端:透過設計公司持續迭代」已與控管/實況脫節。**提案:Now.md 進行中段補這兩條,客戶端段改寫為「七區進場施工」。**

- **🟡 `owner` 值域未統一。** 控管檔混用 `Francis` / `changchen0913` / `擷果創意`,而 vault 正典(Entity_Map)慣稱 `Frncs`。**提案:對齊一份 owner 正名表(Francis=Frncs?),避免 dashboard 依 owner 聚合時被拆成多人。**

—— 以上皆為提案,**未寫入或修改任何檔案**。要我把其中任一條實際落檔(補 frontmatter / 統一 sprint / 更新逾期狀態),再明講。


## Repos 盤點 (`repos-inventory`)

`98 資料/Repos_State` 現況已看完:只有一份 `2026-05-25_inventory.md`,距今約 **54 天**、是單檔快照無 diff。以下為「如何更新盤點」的提案(**唯讀,真正 git 狀態等套用時再抓;未寫任何檔**):

- **🔴 host 欄與現實脫節,新盤點必先釐清掃描主機。** 舊檔 `host: VisTwin (D:\VisTwin)` 全走 Windows 路徑,但本 session 跑在 **macOS `/Users/visionbase/Desktop/VisTwin/vistwin-vault`**。這代表已遷機或雙機並存。**提案:新盤點開頭標明掃描主機;若雙機,branch/dirty 狀態要分 host 區塊,否則兩台的 untracked/sync 會互相污染。**

- **🟠 改成「一次盤點一檔 + 標色遷移對照」,別覆蓋舊快照。** 現在只有單檔、看不出趨勢。**提案:新增 `2026-07-18_inventory.md`,保留 05-25 舊檔;並在 `Repos_State/` 補一份 `README`(index),記每版日期與「🔴/🟡/🟢/🔵 家數」變化,讓盤點變時間序而非定格。**

- **🟠 org rename 會讓所有 remote URL 失效,盤點要先做前後對照。** Now.md 掛著 `metaarchetech → vistwin-tech` org rename 未執行;舊檔每個 repo 的 remote 都是 `github.com/metaarchetech/*`。**提案:新盤點的 remote 欄加「rename 前/後」兩列,套用時一次抓新 URL,避免 rename 後整份 inventory 連結全爛。**

- **🔴 PAT 盲區要欄位化,別讓 cache 推測混成 up-to-date。** 舊檔註明 8 個 VisTwin repo 因 fine-grained PAT 沒授權、sync 只能靠本機 cache 推(且 DASHBOARD_PAT blocker 仍未解)。**提案:抓資料前先確認 PAT scope;新盤點把「即時 truth / cache 推測」拆成明確欄位,`up-to-date` 若是推測要標星號。**

- **🔴 6 個紅項逐一重抓、判是否已解,別照抄 5/25 狀態。** R2 `metaarchetech-site`(behind 3 + dependabot #5/#6)、R3 vault(behind 2)、R4 `visustwin-vision`(feat branch 未回 main)、R5/R6 `WebController`+`welltek-twin`(`claude/docs-bootstrap-all-repos` 各 ahead 1)、R1 `visustwin-extensions`(25 untracked)。**提案:套用時對每個紅項先跑 `git status` / `gh pr list`,已 merge/pull 的直接降色,54 天內極可能多數已變。**

- **🟡 寶鋪「七區進場施工」新產物尚未進盤點視野。** 本週 daily 顯示新增七區走線圖 + 走線統整(MQTT/埠口分配);舊檔「非 git 資料夾」段還停在 `baopu-v0.1` deployment bundle(18MB、無版控)。**提案:新盤點重查這批施工檔落在哪個 repo/是否進版控,並複核 Quartz publish 用的 site repo(承 rsync blocker)有無納入盤點。**

- **🟢 懸置決策(如 `visustwin_yolo` archive)升級為到期待辦,別每版重列。** 綠項「1 年沒動、建議 archive」已列 54 天沒動作。**提案:新盤點把這類重複建議標「逾期 N 天未決」或直接改本機-only,並回頭把 Now.md「Repos 大整理(🔴6 🟡5 🟢1 🔵4)」那行的引用同步成新家數。**

—— 以上皆為提案,**未寫入或修改任何檔案**。要我在套用階段實抓 git 狀態、產出 `2026-07-18_inventory.md` 新快照,再明講。


## 發布盤清 (`publish-audit`)

發布全鏈已盤清(SKILL + 實掃 publish 旗標/敏感目錄/排除清單假設)。以下為提案(**唯讀,未寫任何檔**):

- **🔴 政策與實作矛盾——這是全鏈最大破口。** `index.md` 白紙黑字寫「本站採 **allowlist** 發佈:預設不公開,僅少數標記頁面對外」,但 SKILL §rsync 的實作是 `rsync -av --delete` + **排除式 blocklist**,根本不讀 `publish:` 旗標。**旗標乾淨(全庫僅 `index.md` 一頁 true)只有在 Quartz build 端確為 ExplicitPublish 時才成立;否則宣稱的白名單是假的。提案:先定調到底走哪套,兩者不能並存。**

- **🔴 blocklist 沒涵蓋新增敏感目錄 → `--delete` 會把它們鏡像上站。** 實掃確認 `03 公司/`、`01 專案/寶鋪 showcase/進場施工/`、`02 技術/Design-System/brand/applications/{francis-xu,lu-yen-cheng,chao-chia-wei,chuang-tzu-yang}/印廠下單說明.md`(**含 4 位個人姓名**)全部存在,且**都不在 SKILL 的 exclude 清單**。**提案:套用前把這三類補進 `--exclude`,或改真 allowlist;在 site repo 端(owner: Francis,本 vault 不可驗)核對這批是否已外洩到 `content/`。**

- **🔴 [2] 自動段仍死,現行是手動 rsync 裸奔。** `DASHBOARD_PAT` 未設 → `sync-to-quartz.yml` 為 disabled stub,靠人工 rsync 代跑。**提案:要嘛補設 secret 復原 workflow,要嘛正式承認「手動為唯一主路徑」並把上面兩條排除規則寫死進手動腳本,別每次靠記憶帶 `--exclude`。**

- **🟠 SKILL 的主機/路徑已與現實脫節,pre-flight 會失敗。** SKILL 全篇假設 `D:\Visustwin\obsidian`(Windows + junction),但本 session 跑在 **macOS `/Users/visionbase/Desktop/VisTwin/vistwin-vault`**,repo 名也從 `obsidian` 變 `vistwin-vault`。**提案:更新 SKILL 的 Pipeline [1] 與 Pre-flight 路徑/主機,否則照 SKILL 跑會停在「不在 vault root」。**

- **🟠 org rename 會一次打斷整條鏈的 repo 參照。** Now.md 掛著 `metaarchetech → vistwin-tech` org rename;SKILL 內 `metaarchetech/metaarchetech-vault`、`metaarchetech.github.io`、remote URL、`gh api` 全路徑都會失效。**提案:rename 前先把 SKILL 的 repo 名參數化或標「rename 後對照」,並列為 `revisionTrigger`(SKILL 已寫「repo 遷移到新 org」為觸發條件,現已成立)。**

- **🟡 壞連結兩個真實斷點仍未修,會被帶上站。** `[[…/sprints/2026-W18]]`(W18 資料夾無同名筆記/README)與 `[[…/sprints/2026-W21]]`(有 README 但無 `2026-W21.md`),前幾份 daily 已實掃出、逾 3 天未修。**提案:W18 補 README 或改指 `sprints/README`;W21 改寫成 `[[…/2026-W21/README|2026-W21]]`(W26 已是正確寫法)。完整清單需授權跑一次唯讀 `python3` 全庫 resolver。**

- **🟢 排除清單的 site-only 假設仍成立,但要列為每次必驗。** 實掃確認 vault root 目前無 `graph.md`/`Images/`/`.gitkeep`(僅存在於 site `content/`),故 `--exclude` 這三項仍需保留以免 `--delete` 誤殺。**提案:把這條「假設重驗」寫成手動段 pre-flight 的固定一步;另 SKILL `lastReview`/`lastUsed` 停在 2026-05-20(約 59 天),一併更新。**

—— 以上皆為提案,**未寫入或修改任何檔案**。要我在套用階段實抓 [1]–[4] 即時 sync 狀態、或跑全庫壞連結 resolver,再明講。


## 筆記衛生 (`note-hygiene`)

筆記衛生已實掃(269 份 `.md`;frontmatter 覆蓋、異體字、命名、分區)。以下為修正提案(**唯讀,未寫任何檔**):

- **🔴 異體字「寶鋪/寶舖」全庫對半裂,連正典自己都矛盾。** 內文含「寶舖」68 檔 vs「寶鋪」61 檔;**資料夾名 + 全部 `_控管/*` 用「寶鋪」,但內容筆記(`寶舖品牌概念對照表`、`寶舖C棟_七區走線圖冊`)與 `Entity_Map` 別名映射(§客戶/專案:**寶舖**)用「寶舖」**,且 Entity_Map 第 21 行又滑回「寶鋪知行案」。**提案:先在 Entity_Map 拍板唯一正字(別名映射傾向「寶舖」),再全庫 sed 統一;`[[寶鋪]]` 這類 wikilink 改字會連鎖斷連結,須連同 alias 一起處理。**

- **🟠 「VisTwin」大小寫四種寫法並存於目錄名。** `01 專案/VisTwin`、`02 技術/**Visustwin**`、`04 Github/**Vistwin**Dev`、`99 封存/…/**VisusTwin**` —— 四種 casing。CLAUDE.md/正典是 `VisTwin`。**提案:非封存區統一為 `VisTwin`(封存區凍結不動);目錄改名前先掃全路徑 `[[…]]` 連結,與 GitHub org rename 同一波處理避免二次斷連。**

- **🟠 frontmatter 覆蓋率偏低,需分區補齊而非全庫齊頭。** 269 檔中僅 116 有 `title:`、149 有 `tags:`、112 有 `created:` → 逾半缺 `created`。**提案:先排除 `04 Github/`(11 檔,鏡像 repo 的 README,不該套 vault frontmatter)與 `99 封存/`(25 檔,凍結),對其餘約 233 份「活躍」筆記補最小三欄 `title/created/tags`;`created` 可用 git 首次 commit 日期回填。**

- **🟠 `pm: true` 內容筆記缺 schema 必填欄(與上輪對帳同源)。** 9 份帶 `pm:` 的內容筆記全缺 `type`、部分缺 `status`,`Physical_AI/README.md` 還留舊詞 `status: active`(非 enum)。**提案:與「三處對帳」一併處理——內容筆記要嘛補齊 `type/status`,要嘛去 pm、只由 `_控管/*` 用 `ref` 連回,不要半殘並存。**

- **🟡 日期命名混用三種格式。** `2026-06-25_n8n-stack-ROI.md`(全日期-連字號 + 底線接主題)、`0513_展示設計提案.md`(MMDD 非全日期)、sprint 內 `2026-06-25.md`(純全日期)並存。**提案:定一條命名規約(建議 `YYYY-MM-DD_主題`),`0513_` 這類補成完整年份;純日期 daily 維持現狀。**

- **🟡 孤兒筆記需授權 resolver 全掃,先標結構性熱點。** 全庫反向連結圖(269 檔比對 `[[wikilink]]`)因唯讀 `python3` 執行被授權關卡擋下,無法出完整孤兒清單。**結構上高風險區:`04 Github/` 鏡像 README(天生無 vault 內連入)、`98 資料/` 冷藏區、`寶鋪 showcase/進場施工/` 新增走線 PDF(檔案非筆記、無 `[[]]` 連入)。提案:授權跑一次唯讀 resolver 產出真實孤兒清單,再逐一決定「補連入/歸檔/刪」。**

- **🟡 根目錄散落 4 檔,`_changelog.md` 歸屬待定。** 根層有 `CLAUDE.md`/`Now.md`/`index.md`(皆合理入口)+ `_changelog.md`。前三者屬正典入口應留根;`_changelog.md` 若是 vault 變更日誌,**提案:移入 `00 系統/Meta/` 與維護提案封存同軌,保持根目錄只留入口檔。**

—— 以上皆為提案,**未寫入或修改任何檔案**。要我在套用階段跑授權 resolver 出孤兒清單、或執行異體字/casing 統一(含連結連鎖修正),再明講。



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/302?signature=aa37f315fea15d2936946245184a42d773e90676d825549d40f1ffb41d23fdf2
