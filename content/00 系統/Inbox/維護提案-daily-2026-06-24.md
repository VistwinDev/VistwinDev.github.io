---
title: 維護提案 daily 2026-06-24
type: maintenance-proposal
---

# 🔍 三庫維護提案 — daily 2026-06-24

## daily 日誌 (`daily-journal`)

讀完 Now.md、今日 daily(`2026-06-25.md`)及今天實際 git 變動。今日 daily 主體已相當完整(完成項/在做/blocker/明日都有),以下是**唯讀提案** —— 今天該補但目前缺的:

## 今日 daily 補寫提案(2026-06-25)

**進度(完成項該補一條)**
- **新增 `98 資料/Strategy/2026-06-25_n8n-stack-ROI.md` 策略筆記**(今天 03:33 建,daily 未提)。結論已成形:**n8n 當薄殼編排 + Claude 當大腦**,兩者互補非二選一。標 `P2 / owner: Francis / todo: 做成 VISION BASE 簡報`。這是今天唯一的新知識產出,應入完成項。
- **journal 自動化基建本身今天有推進**:`daily-weekly-journal/SKILL.md` + 4 份 Templates(Daily Standup / Sprint Planning / Sprint Retro / Weekly Review)+ `vault-health.mjs` 今日都有改動。daily 只記了「節奏重啟」,沒記「工具層也同步補齊」——可補一條,標示節奏重建不只是補 log、連模板鏈也修了。

**決策(可回填到既有待決)**
- 「在做項」裡的 **「daily/weekly cron 是否走 n8n」待決,今天其實已有答案方向**:n8n-ROI 筆記把 cron/排程定位成 n8n 的車道(安全韁繩+整合織布)。建議把這條待決從「未決」更新為「方向已定:n8n 薄殼編排,細節待簡報」,並雙向連結到該策略筆記。

**Blocker(維持但需動作)**
- 兩個舊 blocker(`DASHBOARD_PAT`、GitHub org rename)在 daily 已標「逾期需重新標期」,但**今天仍未實際標期/降級**——「明日要做」第一條原封不動搬到了今天。提案:今日就給出明確處置(關閉 / 降 P3 / 改期到具體日),別再讓它滾動。

**收尾(daily 未完成欄位)**
- **心情 / 能量 / 一句話 三欄仍是空占位**(`/10`、`(等使用者填)`)。daily 尚未真正封板,需使用者補填才算完成。

要不要我把以上(尤其前兩條進度 + 決策更新)實際寫進今日 daily?(目前依指示僅提案,未動任何檔。)


## Inbox 分類 (`inbox-triage`)

已讀完 `00 系統/Inbox/` 與相關慣例。Inbox 實質只有**一篇**:`維護提案-daily-2026-06-24.md`(另有空 `.gitkeep`)。以下是**唯讀提案**(歸檔 + frontmatter),未動任何檔。

## Inbox 歸檔提案(2026-06-25)

**這篇的本質問題**
- **`維護提案-daily-2026-06-24.md` 是「昨天三庫維護的產出物」被丟在 Inbox,不是隨手記**。Inbox 應只放「外部進來、未處理的零散筆記」,維護報告不該佔位 —— 否則 Inbox 永遠不會真的清空(這正是健檢「Inbox 0 篇待處理」與現實矛盾的根因)。

**歸到哪個資料夾**
- **建議移到 `00 系統/Meta/maintenance/`**(需新建,目前不存在),與 `Meta/health/` 同層,讓「健檢報告 + 維護提案」兩類自動產出物集中、可批次 glob。若不想新建層,退而求其次放 `00 系統/Meta/`。
- **檔名對齊 health 慣例**:health 是 `health-2026-06-24`,這篇建議改 `maint-daily-2026-06-24`(或全中文固定一種前綴),利於日後掃描。

**該補什麼 frontmatter**
- 目前只有 `title` + `type`,**缺日期與標籤,無法被健檢/查詢索引**。對齊 health 報告慣例(`created` + `tags` 陣列)補:
  - `created: 2026-06-24`
  - `tags: [system, meta, maintenance-proposal, daily]`
  - **保留 `type: maintenance-proposal`**(這欄選得好,建議定為此類產出的標準 type)
- **加 `status: consumed`(或 `archived`)**:對照今天的 daily(`2026-06-25.md`),這篇第 12–28 行的提案幾乎都已落進今日 daily,內容已被消化。不標狀態,日後會被誤當待辦再跑一次 —— 且它寫的是「今天 daily 還沒建」,但今天 daily 已建好,**內容已過期**。

**流程層(根因修補)**
- **改規則:三庫維護的提案預設寫到 `Meta/maintenance/`,別再寫進 Inbox**。這是病灶 —— 不改規則,下一輪維護又會在 Inbox 生一篇,職責永遠不乾淨。
- **`localhost:5678` 核准表單連結(第 77 行)留意有效期**:這篇若移出/改名,該 n8n form webhook 連結會變死連結;搬檔時建議一併確認此提案是否已透過該表單核准結案,結案則可移除連結或標註已失效。

**需求釐清**
- 扣掉這篇產出物,**Inbox 沒有真正的隨手記**。若你近期有桌面/手機隨手記,可能落在外部(Drive / Gmail 草稿 / Apple Notes)而非 vault Inbox。要的話下一輪我可去 Drive/Gmail 撈,再提案歸檔點。

最關鍵一條:**把這篇移出 Inbox、改名對齊 health、補 `status: consumed`** —— 三動作一次到位,Inbox 才真正歸零。需要我實際執行嗎?(目前依指示僅提案,未動檔。)


## 發布快檢 (`publish-quick`)

快檢完成。掃了全庫 `publish:` 旗標、客戶/私密區、以及壞路徑連結。結論:**目前沒有誤公開風險,壞連結是大宗**。以下為**唯讀提案**,未動任何檔。

## 發布健康快檢(2026-06-25)

**誤公開風險 — 低**
- **全庫唯一 `publish: true` 是 `index.md`,乾淨**。內文只有標題+一句 allowlist 說明,連到 Vault Home 是純文字「私有,不公開」**無任何 `[[連結]]`**,不會把內部頁連帶上線。allowlist 模型成立,維持現狀。
- **客戶/私密區零 publish 標記** — `01 專案/`(含寶舖 showcase / welltek)、`03 公司/`(登記/商標/財務/合約)全區掃過,**沒有任何 `publish:` 旗標**,沒有誤標公開。寶舖客戶頁安全。
- **`02 技術/Visustwin/INTEGRATION_PLAN.md` 與 `98 資料/Strategy/2026-06-25_n8n-stack-ROI.md` 都是 `publish: false`** — 安全。後者標明「本檔不公開」,正確;前者內文出現的 "publish" 是 pub/sub 術語,無關發布。

**壞連結 — 大宗(高槓桿一條)**
- **57 條 `技術/VisTwin/` → 應為 `技術/Visustwin/`**(少一個 `us`)。已確認 `02 技術/VisTwin/` 資料夾**不存在**,全是壞連結。散在 **36 個檔**:`GitHub Overview`、`Backlog`、`sprints/README`、近 25 個 Plugin README,連 `01 專案/visionbase/README` 與 `寶鋪 showcase/welltek/README` 也中。**建議全庫批次取代 `技術/VisTwin/` → `技術/Visustwin/`**,一次清掉 ~57 個孤兒連結(對應健檢「243 孤兒連結」的最大可一鍵修區塊)。
- **批次取代的排除項**:`99 封存/Metaarchetech-site/README.md`(legacy 凍結區,別動)、以及 `00 系統/Meta/health/` 與 `Inbox/` 那幾條 —— 它們是健檢報告/提案在**引述**這個壞連結,不是真要連過去,改了反而失真。建議批次範圍限定 `02 技術/`、`01 專案/`、`00 系統/GitHub Overview` + `Templates`。
- **模板佔位符這次乾淨**:`Sprint Planning/Retro.md` 雖在壞連結清單內,但本輪 grep 未抓到 `{{...}}` 與 `VisTwin` 同行的 Templater 變數衝突 —— 批次取代時這兩檔仍要逐條確認,只改 `VisTwin→Visustwin`、保留任何 `{{sprint}}` 佔位符。

**唯一需查證的真風險(非本 vault 可驗)**
- **rsync 是否真按 `publish:` 過濾**:本 vault 根目錄無 quartz/全域發布設定,理論上只 `index.md` 出去;但實際 sync 走手動 rsync(`-av --delete` + 排除式 blocklist,**不看 `publish:` 旗標**)。守門其實落在 site repo 的 Quartz 設定 —— **若用的是 RemoveDrafts 而非 ExplicitPublish,allowlist 形同虛設**。本 vault 無法驗,建議列待辦去 site 端確認。這是目前唯一的發布風險點。

**預防提醒**
- **showcase 對外前先過 guard**:寶舖 showcase 6/22–24 大改,日後若 flip `publish:true` 對外,務必先檢查頁內 `[[連結]]` 不會把 `_控管/`、財務、合約等私密頁帶上線。目前還沒到這步,但很可能很快碰到。

最高槓桿:**批次修 `VisTwin→Visustwin`(57 條,限定範圍排除封存/健檢引述)**;最該查證:**rsync 是否真按 publish 過濾**。需要我實際執行批次取代嗎?(目前依指示僅提案,未動檔。)



---
## ✅ 核准
看完上面,要**實際套用**哪些?點這裡開核准表單(勾選後送出才會寫入/commit):

http://localhost:5678/form-waiting/19?signature=c0a27f46797d77336862cdb64449f211ab0e9aba4f23d9d3910bc23e9d3a7f1e


寫完只回那個檔案的相對路徑。
